# Приём данных

Компонент: всё, что связано с получением данных из Метрики и их сохранением в
хранилище игры — разговор по сети, сборка дня, запись на диск и учёт загруженного.
Реализация на сейчас: `scripts/utils/parquet_store.py` — **запись дня в сырьё**;
`scripts/utils/row_check.py` — **сверка числа строк**; `scripts/utils/load_state.py` —
**учёт загруженных дней и реконсиляция**; `scripts/utils/writer_lock.py` — **замок одного
пишущего**; `scripts/8x_metrica_logs_api/p81_load_logs.py` — **оркестратор приёма за день**
(связывает все шаги в один цикл) и **решение «какие дни грузить» за период** (инкремент +
свежее окно перезалива); `scripts/tools/logs_api_cli.py` (подкоманда `update`) — **команда
обновления одной строкой** (поверхность для оператора: прогон обоих источников, коды
возврата).

> На этом шаге (истории 2.2–2.8) компонент описывает **запись одного дня в сырьё**,
> **сверку числа строк** после записи, **учёт загруженных дней** (журнал «что загружено»
> + сверка журнала с файлами на диске), **замок одного пишущего** (чтобы две записи не
> столкнулись), **оркестратор приёма за день** — тот, кто проводит один день через весь
> путь от заказа выгрузки до записи и учёта, — и **решение «какие дни грузить» за период**
> (пропуск уже загруженных + всегдашний перезалив свежего окна последних дней). Разговор с
> Метрикой по сети описан отдельно в [`metrica-client.md`](metrica-client.md) и здесь не
> повторяется. **Команда запуска одной строкой** (`gdau-logs update`) описана ниже в разделе
> «Команда обновления».

## Что делает

Берёт уже разобранные строки **одного дня одного источника** (источников два:
`visits` — визиты и `hits` — отдельные просмотры/события) и кладёт их в **один файл-
партицию**: `data/raw/<источник>/<дата>.parquet` (например `data/raw/visits/2026-05-20.parquet`).
Один файл — это ровно один день одного источника.

Данные сохраняются **как есть, строками** — ровно так, как пришли от Метрики, без
каких-либо преобразований значений: числа, даты, даже списки (вроде
`[8273645,8273646]`) лежат текстом, ничего не округляется и не обрезается.
Единственное, что меняется при записи, — **названия колонок**: «родные» длинные имена
Метрики (`ym:s:visitID`) заменяются на наши короткие (`visit_id`) по единому справочнику
полей (каталогу схемы). Это переименование — единственное преобразование на входе.

Запись сделана **атомарной**: сначала данные пишутся во временный файл рядом, а потом
одним мгновенным движением он подменяет настоящий файл дня. Поэтому файл дня появляется
**целиком и сразу** — если что-то прервётся посреди записи, на диске не останется
«полу-дня». Если такой день уже был загружен раньше, перезапись просто заменяет **один
этот файл**, не трогая остальные дни.

## Зачем нужен

Это **самый нижний слой хранения**. Принцип простой: сначала сохранить всё, что пришло,
**дословно и без потерь**, а уже разбор по типам и удобные представления построить
поверх этого позже. Если бы данные преобразовывались прямо при записи, любая ошибка
разбора означала бы потерю исходника — а так исходник всегда цел, и его всегда можно
перечитать.

Отдельная ценность — **надёжность обновления базы** (это одно из главных требований
проекта). День должен быть либо полностью на месте, либо отсутствовать — никаких
частично записанных файлов, которые потом примут за настоящие данные. Приём «временный
файл → мгновенная подмена» как раз это и гарантирует, в том числе на Windows.

Ещё одно осознанное решение: файл-партиция пишется **встроенным движком**, который и так
используется в проекте для работы с данными, — без подключения тяжёлых сторонних
библиотек обработки таблиц. Меньше зависимостей — проще и стабильнее.

## Контракт с другими

- **Что принимает на вход.** Имя источника (`visits`/`hits`), дату дня, список колонок
  (родными именами Метрики, в нужном порядке) и сами строки этого дня (значения —
  текстом, как пришли; пустая ячейка допустима). Список колонок задаёт и порядок, и
  схему файла — **даже если строк нет**.
- **Что отдаёт.** Готовый файл-партицию на диске и число записанных строк (его дальше
  использует сверка). Обещания потребителям:
  - один файл = один день одного источника; перезапись дня не трогает другие дни;
  - значения не искажаются и не обрезаются — кроме переименования колонок по каталогу;
  - **валидный пустой день** (Метрика честно вернула ноль визитов) — это **пустой файл
    со схемой**, а не «данных нет» и не ошибка; такой день считается загруженным;
  - смена списка полей со временем не переписывает старые дни задним числом — каждый
    день хранит ровно те колонки, что были у него на момент загрузки.
- **Как сообщает о проблемах.** Понятной остановкой, а не сырой технической ошибкой:
  нет корня хранилища игры → сразу понятное сообщение (и ни одной папки/файла при этом
  не создаётся); незнакомый источник или колонка, которой нет в каталоге → понятная
  ошибка (поле без записи в каталоге считается дефектом); строка не той ширины, что
  заголовок → ошибка, а не молчаливое дополнение/обрезание.

## Сверка числа строк

### Что делает

После того как день записан в файл-партицию, этот шаг **пересчитывает данные на входе и на
выходе и сравнивает их**. На входе он считает, сколько строк-данных прислал источник —
прямо по сырым кускам выгрузки Метрики, **не считая строку-заголовок в каждом куске** (а
заголовок Метрика повторяет в каждом куске, поэтому его вычитают отдельно из каждого, а не
один раз на всё). На выходе берётся число строк, которое реально легло в файл-партицию дня.
Если эти два числа **не совпали** — загрузка останавливается с понятной ошибкой; если
совпали — всё в порядке, и шаг молча пропускает день дальше.

### Зачем нужен

Чтобы **потеря или задвоение строк где-то по пути** — при скачивании, сборке дня или записи
— **сразу останавливала загрузку**, а не просачивалась в базу незаметно. Принцип простой:
лучше громкая остановка, чем тихо неполный день, который потом примут за настоящие данные.
Поэтому источник считается «от первоисточника» (по сырым кускам), независимо от того, как
их разобрала программа: если бы сравнивали «сколько разобрали» с «сколько записали»,
ошибка разбора замаскировалась бы — оба числа упали бы одинаково. Это одно из главных
требований проекта к надёжности обновления базы.

### Контракт с другими

- **Что принимает на вход.** Два числа: сколько строк прислал источник и сколько строк
  записано в файл-партицию (плюс имя источника и дату — только чтобы сообщение об ошибке
  было понятным). Число записанных строк ему даёт соседний шаг записи (он его возвращает).
- **Что отдаёт.** При совпадении — **ничего** (день признан целым, идёт дальше). При
  расхождении — **жёсткую остановку** загрузки (а не предупреждение в логе, которое легко
  проглядеть). Обещания потребителям:
  - совпадение **«ноль на ноль»** (источник честно вернул пустой день) — это **успех**, а
    не «данных нет» и не ошибка;
  - **дубли строк НЕ убираются**: сверяем именно по количеству строк, а данные хранятся
    дословно — если в источнике две одинаковые строки, обе посчитаны.
- **Как сообщает о проблемах.** Понятной остановкой с указанием источника, даты и обоих
  чисел («источник дал столько-то, записано столько-то»), чтобы сразу было видно, где
  разошлось.

## Учёт загруженных дней и реконсиляция

### Что делает

Ведёт **журнал** того, какой день какого источника уже загружен, сколько в нём строк, когда
и в каком он состоянии. И на старте **каждого** обновления делает **реконсиляцию** — сверяет
этот журнал с тем, что **реально лежит файлами на диске**: проходит по всем дням (и тем, что
записаны в журнале, и тем, что есть файлами) и для каждого решает — он действительно
загружен или его надо перелить. По итогу отдаёт список дней, которые **подтверждённо**
загружены.

### Зачем нужен

Чтобы слово «загружено» было **правдой о данных**, а не просто отметкой в журнале. Журнал
может соврать: запись оборвалась посреди дела, файл дня кто-то удалил или он побился, кто-то
вмешался руками. Если доверять только журналу, в базе незаметно окажется «полу-день», который
все принимают за настоящие данные. Поэтому правду назначает **не журнал, а сами файлы**: на
старте обновления журнал каждый раз приводится в соответствие с диском. День, в полноте
которого нет уверенности, честно помечается «перелить» — **лучше лишний раз перелить один
день, чем тихо считать неполный день готовым**. Это часть главного требования проекта к
надёжности: база не должна «сломаться» из-за оборванной загрузки или пропавшего файла.

### Контракт с другими

- **Что принимает на вход.** Готовое соединение с рабочей базой игры (открывает и закрывает
  её **другой** компонент — этот лишь пользуется). Список источников для сверки
  (`visits`/`hits`) — по умолчанию оба.
- **Что отдаёт.** Список **подтверждённо загруженных** дней — его дальше использует
  оркестратор, чтобы решить, какие дни можно пропустить, а какие перелить. Обещания
  потребителям:
  - день считается загруженным **только если совпало всё трое**: файл дня есть **+** в
    журнале статус «загружен» **+** число строк в журнале равно реальному числу строк в
    файле. Любое из трёх не сошлось — день **не** загружен (под перезалив);
  - **источник истины — файл**: при расхождении правят журнал под факт диска, **никогда
    наоборот** (ложная отметка «загружен» не остаётся);
  - **битый или нечитаемый файл одного дня НЕ роняет всё обновление** — этот день просто
    помечается «перелить», а реконсиляция спокойно идёт дальше по остальным дням (один
    сбойный файл не должен заблокировать обновление всей базы);
  - **недописанный день** (отметка «в процессе» от оборванной записи или «сбой») считается
    **незагруженным** — даже если файл на месте и число строк случайно совпало;
  - осиротевший временный файл (`*.parquet.tmp` от прошлого крэша) **не** считается днём —
    это мусор для уборки соседним шагом записи, а не загруженный день.
- **Как сообщает о проблемах.** Расхождения журнала с диском — это **штатная работа**, а не
  ошибка: журнал молча приводится к факту, в лог идёт итог («столько-то дней подтверждено,
  столько-то исправлено»). Битый файл — предупреждение в лог с путём, без остановки
  обновления.

## Замок одного пишущего

### Что делает

Перед любой записью в хранилище ставит **замок «пишу»** — один файл-замок на **всё
хранилище игры** (не на отдельный день). Пока замок держит **живой** процесс записи,
второй процесс записи **сразу останавливается с понятной ошибкой** — он не встаёт в
очередь, ничего не ждёт и ничего не записывает. По окончании записи — **при любом исходе,
успех или ошибка** — замок **снимается сам**.

### Зачем нужен

Встроенная база и файлы-партиции **не переживают двух одновременных писателей**: движок
базы рассчитан ровно на одного пишущего, а запись дня идёт через «временный файл →
мгновенная подмена» — двое наперегонки подменяющих один файл дают непредсказуемый
результат, вплоть до порчи данных. Замок гарантирует, что в каждый момент пишет **ровно
один**. При этом если предыдущий процесс записи **умер** (сбой, принудительное
завершение) — замок **не «залипает» навсегда**: его держит сама операционная система и
отпускает в момент гибели процесса, поэтому следующая запись спокойно его перехватывает.
Это часть главного требования проекта к надёжности: одновременная запись не должна
«сломать» базу, а упавший процесс не должен заблокировать все будущие обновления.

### Контракт с другими

- **Что принимает на вход.** Ничего по сути — замок один на хранилище; путь файла-замка
  он берёт сам из настроек хранилища (для тестов путь можно подставить).
- **Что отдаёт.** «Право писать» на время блока записи. Обещания потребителям:
  - замок берётся **на уровне всего хранилища**, **один раз на весь прогон** обновления, а
    не по каждому дню;
  - занят живым писателем → **немедленная понятная остановка** (а не очередь и не
    ожидание): второй одновременный запуск — это аномалия (двойной старт), её ловят громко
    и сразу;
  - **чтение данных (через MCP) замок НЕ берёт и им НЕ блокируется** — анализировать данные
    можно всегда, даже пока идёт запись; замок сериализует **писателей, не читателей**;
  - замок **снимается при любом исходе** записи (успех, ошибка, прерывание), а упавший
    писатель не оставляет «залипшего» замка — его освобождает сама ОС.
- **Как сообщает о проблемах.** Понятной остановкой с указанием **пути файла-замка** и
  совета «дождись завершения записи или проверь, не висит ли процесс» — а не сырой
  технической ошибкой. Нет корня хранилища игры → сразу понятное сообщение **до** создания
  каких-либо файлов (мусорный замок в репозитории кода не появляется).

Сам замок — **независимый примитив**: он знает только про файл-замок и **не** открывает
рабочую базу, **не** пишет партиции и **не** ведёт журнал. Кто именно пишет под замком
(партиции — запись дня, мета — учёт загруженных) и кто оборачивает им весь прогон —
описано в соседних разделах и у оркестратора приёма (история 2.7).

## Оркестратор приёма за день

### Что делает

За **один день одного источника** (`visits` или `hits`) проходит весь путь приёма от начала
до конца, в **строгом порядке**: **заказывает** выгрузку у Метрики, **дожидается** её
готовности, **скачивает** все куски выгрузки, **собирает** из них строки дня, **записывает**
их одним целым файлом, **сверяет** число строк (сколько прислал источник — сколько легло в
файл), **отмечает** день загруженным в журнале и, наконец, **убирает** выгрузку на стороне
Метрики (освобождает квоту). Всё это делается **под замком одного пишущего** — то есть пока
идёт приём, второй процесс записи к этому хранилищу не подключится.

Сам оркестратор — это **связка уже готовых шагов**, а не новая обработка: запись файла,
сверку, журнал, замок и разговор с Метрикой выполняют отдельные части (см. «Границы»). Его
работа — вызвать их **в правильной последовательности** и довести день до конца или честно
остановиться.

### Зачем нужен

Чтобы день доехал **целиком или никак**. Каждый шаг по отдельности уже умеет своё дело, но
кто-то должен расставить их в нужном порядке и обеспечить главное правило надёжности: **день
считается загруженным только тогда, когда файл уже на месте и сверка числа строк сошлась** —
ни секундой раньше. Поэтому:

- если хоть **один кусок не скачался** — день **не записывается вовсе** (а не «сохраним то,
  что успели»): сначала на руках должны быть все куски, и только потом начинается запись;
- **уборка выгрузки** на стороне Метрики идёт **уже после** того, как день засчитан
  загруженным; если убрать не удалось — это не отменяет загрузку (просто останется занятая
  квота, о чём будет предупреждение);
- если что-то сорвалось **до** того, как день засчитан, — день помечается как «надо
  перелить», и следующий запуск спокойно его догрузит (журнал и сверка с диском это
  обеспечивают).

Это прямое продолжение главного требования проекта к надёжности обновления базы: оборванная
загрузка не оставляет «полу-дня», который потом примут за настоящие данные.

### Контракт с другими

- **Что принимает на вход.** Дату дня и источник (`visits`/`hits`). Оба источника грузятся
  **независимо** — каждый своим прогоном, в свой файл и свою строку журнала; их связь
  (события привязаны к визитам) — это свойство самих данных, а не работа оркестратора, и
  используется уже при анализе.
- **Что отдаёт.** Загруженный день в хранилище (файл-партиция + отметка в журнале) и число
  загруженных строк. Обещания потребителям:
  - **повтор того же дня** — это просто перезапись **одного** его файла; другие дни не
    трогаются (никаких массовых удалений ради обновления);
  - **честно пустой день** (Метрика вернула ноль строк) — это успех: пустой файл со схемой,
    день помечен загруженным, а не «данные отсутствуют» и не ошибка;
  - день **под замком**: одновременный второй запуск приёма к тому же хранилищу — это
    аномалия (двойной старт), её ловят сразу и громко, а не встают в очередь.
- **Как сообщает о проблемах.** Понятной остановкой, а не тихим пропуском:
  - **будущий день** (позже вчера по МСК) грузить нельзя — сразу понятная остановка (Метрика
    не отдаёт сегодняшний день); день молча **не** подменяется на вчерашний;
  - Метрика **отменила** выгрузку или сообщила об **ошибке обработки** — понятная остановка
    с указанием статуса (а не «как будто всё хорошо, просто пусто»);
  - готовность выгрузки **затянулась** — ожидание не вечное: есть верхняя граница по времени
    и предел числа подряд-сбоев опроса, после чего — понятная остановка;
  - **число строк не сошлось** (где-то по пути потеря или задвоение) — **жёсткая остановка**,
    день не засчитывается загруженным.

### Где проходит граница (что делают соседние шаги, а не оркестратор)

Оркестратор ничего из перечисленного **не реализует сам** — он это **зовёт**:

- атомарную **запись** дня одним файлом — шаг записи в сырьё (история 2.2);
- **сверку** числа строк — шаг сверки (2.3);
- **журнал** загруженных дней и сверку журнала с диском — учёт (2.4);
- **замок** одного пишущего (оркестратор лишь берёт его **один раз** на весь прогон) — замок
  (2.5);
- **типизированные представления** данных для анализа — рабочий слой (2.6);
- **разговор с Метрикой** по сети (заказать/опросить/скачать/убрать), включая повторные
  попытки и соблюдение лимитов запросов — [`metrica-client.md`](metrica-client.md).

И отдельно: **решение «какие дни вообще грузить»**, пропуск уже загруженных и перезалив
свежего окна — это отдельный слой над оркестратором (раздел **«Какие дни грузить: инкремент и
hot-window»** ниже); сам оркестратор грузит **переданный** ему день. **Команда запуска одной
строкой** (`gdau-logs update`, коды возврата, объединение источников) — раздел **«Команда
обновления»** ниже. Чтение и анализ данных через MCP — история 3.1.

## Какие дни грузить: инкремент и hot-window

### Что делает

За запрошенный **период** (две даты) и один источник решает, какие дни **реально** грузить, и
проводит их через обычный цикл приёма (оркестратор за день, выше):

- дни, которые уже загружены — **по честному учёту** (журнал, сверенный с файлами на диске), —
  **пропускает** (не перекачивает заново);
- отсутствующие дни — **грузит**;
- **последние несколько дней (по умолчанию три) перезаливает всегда**, даже если они уже были
  загружены, — это «свежее окно».

Все дни периода обрабатываются **под одним замком** на весь прогон — замок берётся один раз в
начале, а не на каждый день.

### Зачем нужен

Чтобы повтор обновления был **дёшев и безопасен**:

- не перекачивать то, что уже лежит, — экономия лимита запросов к Метрике;
- но и не пропустить **доезжающие** данные за свежие дни: Метрика дособирает статистику задним
  числом несколько суток, поэтому поверх обычного «грузим недостающее» добавлено «свежее окно»,
  которое последние дни перезаливает всегда;
- повтор того же периода **ничего не ломает**: перезалив дня — это перезапись **одного** его
  файла (идемпотентно, без массовых удалений), ровно как у оркестратора за день.

Если приоритеты сталкиваются — день и «уже загружен», и «в свежем окне» — **побеждает свежее
окно**: день перезаливается. Так свежие данные всегда подтягиваются.

### Контракт с другими

- **Что принимает на вход.** Период (две даты) и источник (`visits`/`hits`). Конец периода —
  не дальше «вчера по МСК»: дальше Метрика не отдаёт, поэтому конец **тихо подрезается** к вчера
  (с записью в лог), а перевёрнутый период (начало позже конца) — **понятная остановка**.
  Размер свежего окна **настраивается**: ноль — окно выключено (грузим только недостающее),
  отрицательное — **понятная ошибка**. Якорь окна — «вчера по МСК», окно прижато к запрошенному
  периоду (исторический период в прошлом со свежим окном не пересекается — там чистый «грузим
  недостающее»). Все проверки периода и окна — **до** взятия замка (не занимать хранилище ради
  заведомо пустого/неверного периода).
- **Что отдаёт.** Итог прогона: какие дни перезалиты, какие пропущены и сколько всего строк
  загружено. Обещания потребителям:
  - **что считать загруженным** берётся у учёта (раздел про журнал и реконсиляцию выше), а не
    «по наличию файла» — пропускаются только **подтверждённо** загруженные дни;
  - если **какой-то день не загрузился** — прогон **останавливается** (громко, без маскировки);
    уже загруженные к этому моменту дни **остаются на месте**, а повторный запуск спокойно
    до-грузит остаток (недостающие дни) — это и есть «можно перезапустить»;
  - перезапуск **не трогает** уже-загруженные дни вне свежего окна (они снова пропускаются).

### Где проходит граница

- **один день целиком** (заказ → ожидание → скачивание → запись → сверка → учёт → уборка) —
  оркестратор приёма за день (выше); этот слой его **зовёт**, а не повторяет;
- **запись/сверка/журнал/замок/типы** — отдельные шаги (истории 2.2–2.6), зовутся как есть;
- **разговор с Метрикой по сети** — [`metrica-client.md`](metrica-client.md);
- **команда `gdau-logs update` одной строкой**, коды возврата и прогон **обоих** источников
  сразу (объединение результата, частичный сбой → ненулевой код, сообщение про исчерпание
  дневного лимита запросов) — раздел **«Команда обновления»** ниже. Здесь — **функция** на
  один источник.

## Команда обновления (`gdau-logs update`)

### Что делает

Это **команда оператору** — одна строка, которой доводят или обновляют данные игры за период.
Под капотом она ничего не делает сама: для **каждого источника** (по умолчанию сразу обоих —
`visits` и `hits`) зовёт уже описанный слой «какие дни грузить» (инкремент + свежее окно) и
тем самым проводит весь период через приём. Источники прогоняются **последовательно, каждый
под своим замком** (замок берётся и снимается на источник): сначала один, потом второй.

На вход — период (две даты), какой источник(и) обновлять (`visits`/`hits`/оба) и, по желанию,
размер свежего окна. На выход — **человекочитаемая сводка** по каждому источнику (сколько дней
загружено, сколько пропущено, сколько строк) и **код возврата**: ноль, если всё доехало;
ненулевой — если хоть один источник не доехал.

### Зачем нужен

Чтобы рутинное «обнови данные за период» было **одним понятным действием** с предсказуемым
итогом, а не сборкой из десятка ручных шагов. Команда тонкая намеренно: всю надёжность
(целостность, замок, сверку, идемпотентность, инкремент, свежее окно) она **наследует** от
нижних слоёв, а сама добавляет только то, чем удобно пользоваться оператору и агенту — выбор
источников, общий итог и честный код возврата.

### Контракт с другими

- **Что принимает на вход.** Период (две даты), источник (`visits`/`hits`/`both`, по умолчанию
  **both** — это проговорено явно, без молчаливой загрузки одного источника) и необязательный
  размер свежего окна (ноль — окно выключено; по умолчанию три дня). Проверка дат, зажатие конца
  периода на «вчера по МСК», проверка окна — всё это делает нижний слой (см. выше), команда их
  не дублирует.
- **Что отдаёт.** Сводку по источникам и код возврата. Обещания:
  - **оба источника пробуются всегда** — сбой одного **не отменяет** попытку второго (чтобы
    одним запуском продвинуть максимум данных);
  - но **частичный сбой не выдаётся за успех**: код **ненулевой, если не доехал хотя бы один**
    источник, и в сводке прямо видно, какой именно упал;
  - **повтор безопасен**: та же команда ещё раз — уже загруженные дни пропускаются (инкремент),
    свежее окно перезаливается перезаписью одного файла на день (без массовых удалений); база не
    дублируется и не ломается;
  - **прерывание не оставляет «висящего» замка**: при остановке посреди работы замок снимается
    (сам по завершении блока записи, а упавший процесс освобождает его силами ОС), а недогруженный
    период спокойно дотягивается повторным запуском.
- **Как сообщает о проблемах.** Понятным сообщением и ненулевым кодом, без «технической
  простыни». Если работа оборвалась или упёрлась в **дневной лимит запросов** к Метрике —
  команда прямо подсказывает: повторите ту же команду, уже загруженные дни пропустятся, остаток
  дотянется позже. Прерывание оператором (Ctrl+C) — это тоже понятное сообщение и ненулевой код,
  а не трассировка.

### Где проходит граница

- **выбор дней, инкремент и свежее окно** на один источник — слой «какие дни грузить» (выше);
  команда его **зовёт** по каждому источнику, а не повторяет;
- **один день целиком**, запись/сверка/журнал/замок/типы — нижние шаги (истории 2.2–2.7);
- **разговор с Метрикой по сети** — [`metrica-client.md`](metrica-client.md);
- сама командная поверхность (разбор аргументов, выбор источников, общий итог и код возврата) —
  это **тонкий слой** в `scripts/tools/logs_api_cli.py` (см. также [`cli.md`](cli.md)); решение
  «один замок на оба источника» сознательно **не** вводится — в модели «один оператор» выгоды нет,
  а каждый день и так атомарен под замком.

## Границы

Сейчас компонент приёма умеет шесть шагов: **атомарно записать один день в сырьё**,
**жёстко сверить число строк** источника с записанным, **вести учёт загруженных дней с
реконсиляцией** журнала против файлов на диске, **ставить замок одного пишущего** вокруг
записи, **связывать всё это в один цикл приёма за день** (оркестратор) и **решать, какие дни
грузить за период** (инкремент + свежее окно перезалива). Он **сам не открывает** рабочую базу
игры (получает готовое соединение — открывает и закрывает базу отдельный компонент) и **не**
разбирает типы. Каждый нижний шаг знает своё место: сверка сама **не** помечает день
загруженным — она лишь **разрешает или запрещает** эту отметку; учёт сам **не** грузит дни — он
лишь говорит правду об их состоянии; замок сам **не** решает, *что* и *когда* писать — он лишь
гарантирует «писатель ровно один»; оркестратор за день сам **не** реализует ни один из этих
шагов — он лишь зовёт их в правильном порядке и грузит **переданный** ему день; а слой «какие
дни грузить» сам **не** пишет и **не** сверяет — он только выбирает дни и зовёт оркестратор за
день под одним замком. Граница с соседями:

- разговор с Метрикой по сети (заказать/опросить/скачать/убрать выгрузку), повторные попытки
  и лимиты запросов — описан в [`metrica-client.md`](metrica-client.md);
- открытие и закрытие рабочей базы игры (куда ложится журнал загрузок) — история 2.1;
- типизированные представления данных, разбор типов и парсинг списков (`TRY_CAST`) —
  история 2.6;
- интеграционная проверка «чтение через MCP не блокируется во время записи» — история 3.1
  (там появляется канал чтения);
- **командная поверхность** обновления — `gdau-logs update` одной строкой, коды возврата,
  прогон **обоих** источников (каждый под своим замком, последовательно), объединение результата
  и сообщение про исчерпание дневного лимита запросов — описана выше в разделе **«Команда
  обновления»** (тонкий слой в `scripts/tools/logs_api_cli.py`; здесь, в слое приёма, — **функция**
  на один источник, а не команда; учёт лишь сообщает, какие дни подтверждённо загружены — решает и
  грузит слой «какие дни грузить»).

Здесь — надёжный нижний слой приёма: дословная запись одного дня одним целым файлом,
немедленная проверка, что ни одна строка по пути не потерялась и не задвоилась, честный
учёт — сверка журнала «что загружено» с тем, что реально лежит файлами на диске, — замок,
гарантирующий, что в хранилище в каждый момент пишет ровно один процесс, оркестратор,
который проводит один день через весь этот путь в правильном порядке: целиком или никак, и
слой «какие дни грузить», который за период выбирает недостающие дни и свежее окно и проводит
их через оркестратор под одним замком — дёшево и идемпотентно.
