# Story 2.7: Оркестратор приёма за день — p81 полный цикл

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a оператор юнита,
I want оркестратор полного цикла Logs API для одного дня/источника,
so that день доезжал до сырья атомарно, под локом, со сверкой строк и чекпойнтом — а реальный контракт API проверялся end-to-end.

**Контекст эпика.** Седьмая история Epic 2 «Приём данных и безопасное обновление хранилища» — **точка сборки**: все примитивы 1.x/2.1–2.6 уже стоят, p81 склеивает их в один связный путь записи дня. Готовое (потребляется, не переписывается): `metrica_client` (1.3 — цикл Logs API, retry/rate-limit), `dates` (1.4 — clamp/формат/`moscow_yesterday`), `catalog` (1.5 — список полей выгрузки), `env_reader` (1.2 — креды), `parquet_store.write_partition` (2.2 — атомарная запись дня), `row_check` (2.3 — жёсткая сверка строк), `load_state` (2.4 — мета + реконсиляция), `writer_lock` (2.5 — лок одного писателя), `database_manager` (2.1 — соединение), `views.create_views` (2.6 — типизированные view; `ready-for-dev`, зависимость). Покрывает **FR-1** (оркестрация полного асинхронного цикла create→poll→download→clean; fail на `processing_failed`/`canceled`) и **FR-3** (оба источника visits+hits независимо; связь `watch_ids`↔`watch_id`). Место в упорядоченной цепочке: 2.7 = **один день один источник**; 2.8 = инкремент/hot-window (решение «какие дни» + диапазон); 2.9 = команда `gdau-logs update` (поверхность). После 2.7 один день реально доезжает от API до сырья под локом со сверкой и чекпойнтом.

**Почему p81 — это «склейка», а не новая логика (корень требования).** Каждый примитив 2.1–2.6 написан как **независимый, тестируемый в одиночку** кусок (conn инъектируется, сети нет, БД сам не открывает). Ни один не знает про асинхронный цикл Logs API и про порядок операций «download → собрать день → temp→rename → сверка → мета → clean». p81 — единственное место, где этот **порядок** материализуется (architecture.md:379–381 «протокол идемпотентного дня»). Вся «тяжёлая» механика (retry/rate-limit, атомарность, сверка-fail, реконсиляция, лок) уже реализована — p81 её **вызывает в правильной последовательности**, а не реализует заново (project-context anti-pattern «реимплементация retry/rate-limit вне клиента»).

**2.7 — ПЕРВАЯ end-to-end история против реального Logs API после 1.6.** Истории 2.1–2.6 в сеть не ходили (live-smoke неприменим — записано в каждой). p81 **и есть** реальный цикл Logs API: create→poll→download. Значит правило project-context «тесты внешнего API — не только моки: обязателен opt-in live-smoke» **снова в силе** (как 1.3/1.6). Именно здесь подтверждаются неподтверждённые предпосылки соседей: реальный формат TSV (разделитель, заголовок-на-часть), формат массивов и их экранирование (опровергнутая предпосылка AC #8 в 2.6), статусы асинхронного цикла. **Live-smoke обязателен; фикстуры offline освежаются из реального ответа** ([[realapi-smoke-tests]], defer 2.3 `deferred-work.md:35`, defer 2.6 примечание к AC #8).

**Это НЕ вендоринг — свой оркестратор.** В дереве архитектуры `p81_load_logs.py` — **своя** оркестрация (architecture.md:424 «**своя** оркестрация (Parquet, не DuckDB-CAST)»). У directaiq `p81_load_logs.py` наследует `BaseScript`, грузит CSV→DuckDB с `CAST` в самой таблице, держит `ym:s:*`-имена, тащит `config_manager`/goal-плейсхолдеры/`_force_drop_tables(DROP TABLE)`, параллельную очередь и «сверку как warning» (`directaiq .../p81_load_logs.py:783 "Расхождение: …" — warning!`). Наша модель — Parquet-партиции + snake_case + жёсткий fail + ноль инфры — **осознанное отличие**. Форму (`_create_parser`, поэтапный цикл create→wait→download) можно держать узнаваемой, но `BaseScript`/`config_manager`/`DROP TABLE`/параллель/warning-сверку **не тащить** (NFR-6). Прямого построчного аналога нет — пиши по контракту ниже. Дерево тестов архитектуры называет `test_p81_orchestrator.py` (architecture.md:484).

### Главные риски / решения (читать до кода)

> ✅ **РЕШЕНИЕ ШЕФА (2026-05-25): вариант A — УТВЕРЖДЁН.** Контракт ниже написан под **вариант A** (`load_day(conn, client, …)` инъектируемое ядро + run-level `ingest_day(source, date)` с одним захватом лока). **Реализуй вариант A.** Разобранная отклонённая альтернатива (вариант B — 2.7 даёт только чистое `load_day`, весь lock/run-level отложен в 2.8/2.9) оставлена в риске №1 для трассируемости решения. Всё остальное (порядок цикла, сплиттер, live-smoke, границы) от выбора A/B **не зависело**.

1. **Декомпозиция 2.7 и scope `.writer.lock`: ядро `load_day(conn, client, …)` (инъекция) + run-level `ingest_day(source, date)` (лок один раз). ✅ ВАРИАНТ A УТВЕРЖДЁН ШЕФОМ (2026-05-25).**

   **Почему вообще вопрос.** `writer_lock` (2.5) **не реентерабелен** (`writer_lock.py:82` docstring: «повторный захват того же пути тем же процессом конфликтует сам с собой → `WriterLockHeldError`»). Архитектура и docstring 2.5 фиксируют: лок берётся **один раз на весь прогон обновления — p81 (2.7)** (`writer_lock.py:19`, architecture.md:384). Значит лок **не** может браться внутри per-day-цикла, иначе 2.8 (диапазон дней под одним локом) словит сам-с-собой конфликт. Поэтому держатель лока — **тот, кто зацикливает дни**, а не сам день.

   **Вариант A (РЕАЛИЗОВАТЬ при подтверждении Шефа).** Два уровня:
   - **`load_day(conn, client, source, date, *, catalog=None, poll-параметры) -> int`** — **ядро цикла дня на инъектируемых `conn` + `client`**. НЕ берёт лок, НЕ открывает БД, НЕ строит клиент, НЕ ходит в сеть retry-логикой (это клиент). Делает: `mark_loading` → `create_log_request` → poll до `processed` → download всех частей → парсинг TSV → `write_partition` (2.2) → `verify_row_count` (2.3) → `mark_loaded` (**точка коммита**) → `clean`. Возвращает число строк. **Это главный тестируемый шов** (как `load_state`/`views`: conn инъектируется → in-memory DuckDB + поддельный клиент, без сети и без лока).
   - **`ingest_day(source, date, *, …) -> int`** — **run-level единичного дня**: `with writer_lock(): with DatabaseManager.connection() as conn:` строит клиент из `env_reader`, `ensure_load_state_table(conn)` + `create_views(conn)` (один раз), затем `load_day(conn, client, source, date)`. Это и есть AC #3 (запись под локом) + AC #1 (запускаемый p81 для одного дня). Ad-hoc единичный прогон.

   **2.8 НЕ зовёт `ingest_day` в цикле** (реентрантность!) — 2.8 берёт лок/conn/клиент **один раз** и зовёт **`load_day`** напрямую по вычисленному набору дней (reconcile→skip+hot-window). Контракт ниже это фиксирует в docstring `load_day`/`ingest_day`. Так лок-scope = «зацикливающий вход» (для 2.7 это `ingest_day`-один-день; для 2.8 — её диапазон-вход).

   **Вариант B (ОТКЛОНЁН Шефом 2026-05-25).** 2.7 отдаёт **только** `load_day(conn, client, …)`, а run-level (лок + conn + клиент + ensure_table + create_views) **целиком** в 2.8/2.9. Плюс: 2.7 — чистая библиотечная функция, минимум поверхности. Минус: AC #1 «когда **запускается** p81» и AC #3 «p81 **пишет** под локом» в 2.7 не имеют прод-входа — демонстрируются лишь тестом (load_day под локом в тесте). Эффективно переносит интеграцию в 2.8. **Отклонён** — Шеф выбрал A (AC #1/#3 демонстрируются end-to-end уже в 2.7 + есть ad-hoc единичный прогон). Контракт `load_day` идентичен в обоих, отличие — наличие `ingest_day` в 2.7.

2. **КРИТИЧНО (cross-component, defer 2.3): сплиттер записей p81 ОБЯЗАН совпасть с `count_part_rows` (2.3) — единый сплиттер.** `row_check.count_source_rows` считает `expected` = строки-данные сырого TSV (`split("\n")`, `rstrip("\r")`, отбрасывая пустые, минус **один заголовок на часть** — `row_check.py:76`). `actual` = возврат `write_partition` = число **распарсенных** p81 строк. Если сплиттер p81 разойдётся с `count_part_rows` (например, `csv`-модуль, склеивающий встроенный `\n` в значении; или `splitlines()`, режущий `\v`/`\f`/`\x85`/LS/PS), сверка даст **misfire**: ложный жёсткий fail на валидном дне **или** (если оба ошибаются одинаково) маскировку реальной потери. **Решение (defer 2.3 `deferred-work.md:35`): единый сплиттер.** Вынеси в `row_check` публичный хелпер `split_tsv_rows(part: bytes | str) -> list[str]` (декод UTF-8; `split("\n")`; `rstrip("\r")`; отбрасывание пустых — РОВНО логика `count_part_rows.py:71–76`), перепиши `count_part_rows` через него (`max(0, len(split_tsv_rows(part)) - 1)` — поведение неизменно, регресс-тесты 2.3 зелёные), и p81 использует **тот же** хелпер: `lines = split_tsv_rows(part)`, `header = lines[0]`, `data = lines[1:]`, ячейки = `line.split("\t")`. Контракт-тест: часть со встроенным `\r`/CRLF/хвостовым переводом строки/пустой строкой → число распарсенных p81 строк-данных **==** `count_part_rows(part)`. Так `expected` и парсинг согласованы по границам строк, а сверка ловит реальную потерю на участке **download→сборка дня→запись** (потеря целой части, дроп строки до `write_partition`). **Экранирование встроенных `\t`/`\n` в значениях `yandex-docs` не подтверждает → фиксируем простой `\t`/`\n`-сплит сейчас, подтверждаем live-smoke 2.7** (примечание AC #1, defer 2.3).

3. **Механизм загрузки модуля: digit-префикс `8x_metrica_logs_api` (defer 1.1 `deferred-work.md:6`).** Каталог `scripts/8x_metrica_logs_api/` начинается с цифры → `import scripts.8x_metrica_logs_api.p81_load_logs` как **statement** = `SyntaxError`. Но `importlib.import_module("scripts.8x_metrica_logs_api.p81_load_logs")` (строкой) **работает** — идентификатор валидной точечной нотации не требуется. В каталоге **нет** `__init__.py` (только `.gitkeep`) → неявный namespace-пакет, `import_module` отрабатывает. **Следствия:** (а) тесты импортируют p81 через `importlib.import_module(...)`, не через `from scripts.8x_… import …`; (б) будущий CLI `update` (2.9) импортирует p81 так же; (в) **прямого entry-point у p81 НЕТ** (`pyproject [project.scripts]` = `gdau-logs`→`logs_api_cli`, `gdau-init`→`init_project`; p81 дёргает 2.9, не консоль). Сам модуль — обычный: `from __future__ import annotations` первой строкой, абсолютные импорты `from scripts.utils.… import …`. Спека `deferred-work.md:6` упоминала `spec_from_file_location` как опцию — **не нужно**: `import_module` проще и достаточно (`[[simplicity-first]]`).

4. **LIVE-SMOKE ОБЯЗАТЕЛЕН (project-context, [[realapi-smoke-tests]]) + освежение фикстур.** 2.7 — реальный цикл Logs API → `test_p81_orchestrator_live.py` с `@pytest.mark.live` (как `test_metrica_client_live.py`/`test_logs_api_cli_live.py`; `addopts="-m 'not live'"` → в CI отсеян, явный `uv run pytest -m live`). Минимально: **узкое окно в 1 день** ≤ «вчера по МСК», источник `visits` (опц. `hits`), креды из `.env` хранилища → **`pytest.skip`** с понятной причиной если кредов/`GDAU_DATA_ROOT` нет (не ложный красный). Гоняет **полный реальный цикл** `ingest_day`/`load_day` против реального API (create→poll→download→write→verify→load_state→clean), уважая rate-limit (≤5000/day) и асинхронный poll (~30s). **Проверяет реальный контракт:** имена/типы полей ответа совпадают с каталогом; формат TSV (разделитель, заголовок-на-часть); статусы цикла. **Освежить offline-фикстуры из реального ответа:** `tests/fixtures/logs_visits_sample.tsv` уже есть (3-колоночный мини); **добавить `logs_hits_sample.tsv`**; при возможности обновить из реального прогона (формат массивов, экранирование строковых массивов — неподтверждённое в 2.6). Закрывает defer 2.3 и подтверждает решение 2.6 вариант A.

5. **Порядок операций и точка коммита дня (AC #1, #7) — протокол идемпотентного дня (architecture.md:379–381).** В `load_day`, СТРОГО: `mark_loading(conn,source,date)` → `create_log_request` → poll → **download ВСЕХ частей в память** → парсинг TSV → `write_partition(...)` (temp→`os.replace`, атомарно) → `verify_row_count(expected, actual, source=, date=)` → `mark_loaded(conn,source,date,row_count)` **= ТОЧКА КОММИТА** → `clean_log_request`. День «загружен» ТОЛЬКО после rename + сверка + `mark_loaded` (architecture.md:380). **`clean` ПОСЛЕ коммита (AC #7):** если `clean` упал (`RuntimeError` клиента) уже **после** `mark_loaded` → это **WARNING** (остаток квоты), день **остаётся загруженным**, без отката и без fail. **`clean` — не часть коммита.**

6. **Частичные части → день НЕ персистится (AC #6): download всё-или-ничего в память.** Сначала скачать **ВСЕ** части (`download_log_request_part` → `bytes`) в список **в памяти**; только если **все** успешны → парсинг + `write_partition`. Любая часть упала (`RuntimeError`/`OSError`) → исключение **ДО** `write_partition` → ничего не записано (не «собрали что есть», AC #6). Части держим **байтами в памяти** (не россыпью `.tsv`-файлов, как ad-hoc `download` CLI 1.6) → нет мусора частичных файлов (defer 1.6 `deferred-work.md:27`). Объём — части одного дня; для single-day приемлемо.

7. **Дисциплина poll (AC #5): ограниченный цикл, без вечного ожидания.** Poll `get_log_request(req_id)` циклом: интервал ~30s, **верхняя граница** ожидания, **лимит подряд-ошибок** → fail с диагностикой. Значений `config_manager` у нас НЕТ (не тащим, NFR-6) → **модульные константы** `POLL_INTERVAL_S=30.0`, `POLL_TIMEOUT_S=3600.0` (60 мин, как directaiq `_wait_for_request`), `MAX_CONSECUTIVE_POLL_ERRORS=5` + **переопределяемые keyword-параметры** (тесты дают крошечные значения). Статусы: `processed`→дальше; `canceled`/`processing_failed`→fail с диагностикой (AC #2); прочие (`created`/`processing`/…)→продолжать poll до таймаута. Ошибка `get_log_request` (клиент уже отретраил транзиент — поднятое наружу терминально) → счётчик подряд-ошибок, бэкофф, fail на лимите. **rate-limit/retry заново НЕ реализуем** (клиент, NFR-3). **Сон — через инъектируемый шов** (`sleep`-параметр со `default=time.sleep` ИЛИ poll-интервал `0.0` в тестах) — реальные 30s в offline-тестах недопустимы.

8. **Пустой день без частей → колонки из каталога (AC косвенно, граница 2.2 «валидный пустой день»).** API вернул `processed` с **0 частей** (честно пустой день) ИЛИ части только с заголовком → `expected = count_source_rows(parts) = 0`. Чтобы записать **пустую типизированную партицию** (2.2 AC #7), `write_partition` нужны `columns`: при наличии частей — **заголовок TSV** (родные имена); при **0 частей** — `catalog.metrica_fields(source)` (тот же список, что заказан). `rows=[]` → `write_partition` пишет пустую партицию со схемой, возвращает `0`; `verify_row_count(0,0)` → успех; `mark_loaded(...,0)`. День помечен загруженным, **не** «отсутствует»/ошибка. (Совпадает с `parquet_store` AC #7 и `row_check` «0==0 успех».)

9. **Источник колонок = заголовок TSV (родные имена), не переупорядочивать (граница 2.2).** `write_partition(columns=...)` ждёт **родные имена Метрики** в порядке TSV-заголовка (`parquet_store.py:60` — «задают и схему, и порядок»; он сам мапит→snake_case по каталогу, неизвестная колонка→`ValueError`). Бери заголовок **как пришёл** из части (он авторитетен для выравнивания колонка↔ячейка) — он обязан совпасть с `catalog.metrica_fields(source)` (что заказали); `write_partition` fail-loud ловит дрейф. Не переименовывать в p81 (это забота `parquet_store`), не держать `ym:s:*` в логике сверх передачи в `write_partition`.

10. **Граница дат: НЕ молчаливый clamp одиночного дня.** Для одного дня `date1=date2=date`. Голый `clamp_date_range(date,date)` при `date > вчера` **сдвинул бы день** на вчера (загрузили бы НЕ тот день — тихий баг). Поэтому в `load_day`/`ingest_day`: **валидировать** `parse_date(date) <= moscow_yesterday()` → иначе fail-loud `ValueError` (Logs API требует `date2 < today`; будущий день грузить нельзя). Диапазонный clamp (срезать хвост диапазона на «вчера») — забота 2.8 (`clamp_date_range` на границах диапазона). p81 формат дат — `format_date` (1.4), строго `YYYY-MM-DD`.

11. **Связь visits↔hits (AC #4) — факт данных, не код p81.** Оба источника грузятся **независимо** (свой цикл, своя партиция `data/raw/{source}/`, своя строка `load_state`). Связь `visits.watch_ids` ↔ `hits.watch_id` — свойство **каталога/модели** (поля хранятся, джойнятся в SQL агента через view 2.6), p81 **спецлогики связи не несёт**. AC #4 = «каждый грузится независимо; связь через `watch_ids↔watch_id`» → p81 просто умеет оба источника (тот же `load_day` с `source∈{visits,hits}`), связь зафиксируй в docstring/спеке.

12. **На любой fail между `mark_loading` и `mark_loaded` → `mark_failed` (best-effort) + raise (AC #2, опора реконсиляции 2.4).** Оберни цикл дня `try/except`: исключение (poll-fail/download-fail/`RowCountMismatchError`/прочее) → `mark_failed(conn,source,date)` (защитная отметка; реконсиляция 2.4 трактует `loading`/`failed` как незагруженный) + **re-raise** (наружу non-zero). `mark_failed` сам может упасть, если сбой — в `conn`/БД → оборачивай его `contextlib.suppress`/best-effort (вторичная ошибка не должна маскировать исходную); даже если `mark_failed` не прошёл, остаётся `loading` от `mark_loading` → реконсиляция всё равно перельёт (двойная страховка). **`RowCountMismatchError` (2.3) НЕ глушить** — это жёсткий fail целостности (наружу non-zero).

## Acceptance Criteria

1. **Given** дата + источник, **When** запускается p81, **Then** полный цикл: `create` → poll до `processed` → `download` частей → парсинг TSV → `parquet_store` (2.2) → сверка (2.3) → `load_state` (2.4) → `clean`.
2. **Given** статус `processing_failed`/`canceled`, **When** опрашивается запрос, **Then** fail с диагностикой, без молчаливого пропуска.
3. **Given** любая запись, **When** p81 пишет, **Then** под `.writer.lock` (2.5).
4. **Given** оба источника, **When** запускаются visits и hits, **Then** каждый грузится независимо; связь через `watch_ids`↔`watch_id` (FR-3).
5. **Given** poll, **When** ожидание затягивается, **Then** интервал ~30s, верхняя граница ожидания и лимит подряд-ошибок → fail (значения из конфига; rate-limit/retry — из клиента, не заново).
6. **Given** скачивание частей, **When** хотя бы одна часть не скачалась, **Then** день НЕ персистится (никаких частичных дней) — fail целиком, а не «собрали что есть». _[edge-case: частичные parts]_
7. **Given** точка коммита дня = запись `load_state` (после rename+сверки), **When** `clean` падает уже после коммита, **Then** это WARNING (квота-остаток), день остаётся загруженным — не откат и не fail. _[edge-case: clean после коммита / порядок операций]_

> **Примечание к AC #1 (формат TSV — подтверждается live-smoke).** Парсинг TSV в p81 фиксирует **простой** контракт: единый сплиттер строк с `row_check` (риск №2), разделитель ячеек `\t`, заголовок в каждой части. Экранирование встроенных `\t`/`\n` в значениях `yandex-docs` не подтверждает — **реальный wire-формат подтверждает live-smoke 2.7** (риск №4, defer 2.3 `deferred-work.md:35`); offline-фикстуры освежить из реального ответа.

> **Примечание к AC #3/#5 («из конфига», scope лока).** `config_manager` не тащим (NFR-6) → poll-параметры (интервал/таймаут/лимит ошибок) — модульные константы + переопределяемые kwargs (риск №7). Scope `.writer.lock` — **один захват на зацикливающем входе** (для 2.7 один день — `ingest_day`; для диапазона — 2.8), `writer_lock` не реентерабелен (риск №1).

## Tasks / Subtasks

- [x] **Task 1 — `scripts/8x_metrica_logs_api/p81_load_logs.py`: оркестратор цикла приёма за день (AC: #1–#7)**
  - [x] `from __future__ import annotations` первой строкой. Русский модульный docstring: роль (оркестратор полного цикла Logs API за **один день один источник**: create→poll→download→парсинг TSV→`write_partition`→сверка→`load_state`→clean; протокол идемпотентного дня; точка коммита = `mark_loaded`; всё-или-ничего на частях; bounded poll). **Явно**: это **склейка** примитивов 1.x/2.1–2.6, НЕ их повтор; retry/rate-limit живёт в клиенте (NFR-3); это **своя** оркестрация, НЕ построчный вендоринг directaiq (нет `BaseScript`/`config_manager`/`DROP TABLE`/параллели/warning-сверки). Границы: scope `.writer.lock` — зацикливающий вход (риск №1); «какие дни/skip/hot-window» — 2.8; поверхность `update` — 2.9; типизация view — 2.6. Импорты: `import importlib`? нет — это сам модуль; `import logging`, `import time`, `from collections.abc import Iterable/Sequence`, `import duckdb` (аннотация `conn`), `from scripts.utils.metrica_client import MetricaClient`, `from scripts.utils.dates import moscow_yesterday, parse_date, format_date`, `from scripts.utils.catalog import Catalog, load_catalog, VALID_SOURCES`, `from scripts.utils.parquet_store import write_partition`, `from scripts.utils.row_check import count_source_rows, verify_row_count, split_tsv_rows` (см. Task 2), `from scripts.utils.load_state import ensure_load_state_table, mark_loading, mark_loaded, mark_failed`, `from scripts.utils.writer_lock import writer_lock`, `from scripts.utils.database_manager import DatabaseManager`, `from scripts.utils.env_reader import read_metrica_credentials`, `from scripts.utils.views import create_views`. `logger = logging.getLogger(__name__)`. `__all__`. Константы poll: `POLL_INTERVAL_S=30.0`, `POLL_TIMEOUT_S=3600.0`, `MAX_CONSECUTIVE_POLL_ERRORS=5`.
  - [x] **Ядро (инъекция, тестируемо) — `load_day(conn, client, source, date, *, catalog=None, poll_interval_s=POLL_INTERVAL_S, poll_timeout_s=POLL_TIMEOUT_S, max_consecutive_errors=MAX_CONSECUTIVE_POLL_ERRORS, sleep=time.sleep) -> int`** (финализируй сигнатуру под тесты): `conn`/`client` инъектируются (НЕ открывает БД, НЕ строит клиент, НЕ берёт лок — риск №1). Шаги СТРОГО (риск №5): валидация `parse_date(date) <= moscow_yesterday()` fail-loud (риск №10); `_require_valid_source(source)`; `mark_loading`; `fields = (catalog or load_catalog()).metrica_fields(source)`; `create_log_request(date1=date, date2=date, fields, source)` → `req_id` (defensive `resp.get("log_request") or resp` → `["request_id"]`, `int()`); `_poll_until_processed(client, req_id, …)` (риск №7); `parts_bytes = _download_all_parts(client, req_id, info)` всё-или-ничего (риск №6); `columns, rows = _parse_parts(parts_bytes)` или при 0 частей `columns=fields, rows=[]` (риск №8, №9, единый сплиттер риск №2); `expected = count_source_rows(parts_bytes)`; `actual = write_partition(source, format_date(parse_date(date)), columns, rows, catalog=catalog)`; `verify_row_count(expected, actual, source=source, date=date)`; `mark_loaded(conn, source, date, actual)` **= коммит**; `clean_log_request(req_id)` в `try/except (RuntimeError, OSError)` → WARNING, день остаётся (AC #7). Весь блок (от `mark_loading` до коммита) в `try/except BaseException` → `mark_failed` best-effort (`contextlib.suppress`) + re-raise (риск №12). Лог INFO на ключевых шагах. Возврат — число строк.
  - [x] **Run-level (вариант A) — `ingest_day(source, date, *, catalog=None, …poll-параметры…) -> int`:** `with writer_lock():` (AC #3) `with DatabaseManager.connection() as conn:` (write-conn 2.1) `creds = read_metrica_credentials(); client = MetricaClient(token=creds.token, counter_id=creds.counter_id)`; `ensure_load_state_table(conn)` + `create_views(conn)` (один раз); `return load_day(conn, client, source, date, catalog=catalog, …)`. **НЕ зовётся 2.8 в цикле** (реентрантность лока — риск №1); зафиксируй в docstring «2.8 берёт лок один раз вокруг диапазона и зовёт `load_day` напрямую».
  - [x] **Хелперы:** `_poll_until_processed(client, req_id, *, poll_interval_s, poll_timeout_s, max_consecutive_errors, sleep) -> dict` — цикл по `time.monotonic()` граница `poll_timeout_s`; `info = client.get_log_request(req_id)`; `status=='processed'`→вернуть `info`; `status in {'canceled','processing_failed'}`→`RuntimeError` с диагностикой (AC #2); иначе `sleep(poll_interval_s)`; `except RuntimeError`→счётчик подряд-ошибок, `sleep`, на лимите→`RuntimeError`; по таймауту→`RuntimeError` (риск №5/№7). `_download_all_parts(client, req_id, info) -> list[bytes]` — `parts=info.get("parts",[])`; для каждой `download_log_request_part(req_id, part["part_number"])`→в память; любая ошибка пробрасывается (риск №6); 0 частей→`[]`. `_parse_parts(parts_bytes) -> tuple[list[str], list[list[str]]]` — единый `split_tsv_rows` (риск №2): на каждую часть `lines=split_tsv_rows(part)`, `header=lines[0].split("\t")`, `data += [ln.split("\t") for ln in lines[1:]]`; вернуть (header первой части, все data); пустой ввод обрабатывает вызывающий (риск №8). `_require_valid_source` (переиспользовать приём — `VALID_SOURCES`).
  - [x] **НЕ делать:** реализовывать retry/rate-limit/poll-бэкофф API заново (клиент 1.3, NFR-3 — риск №7); `DROP TABLE`/удаление партиции ради перезалива (directaiq-анти-паттерн; перезалив = `write_partition` одного файла, 2.2); сверку как warning (`RowCountMismatchError` наружу — риск №12); брать `.writer.lock` внутри per-day-цикла (`load_day` без лока; лок — `ingest_day`/2.8, не реентерабелен — риск №1); молчаливый clamp одиночного дня (валидация, не сдвиг — риск №10); россыпь частичных `.tsv` (части в памяти — риск №6); тащить `BaseScript`/`config_manager`/goal-плейсхолдеры/параллельную очередь/`view_builders` directaiq (NFR-6); `import scripts.8x_metrica_logs_api…` как statement (digit-префикс — риск №3); парсить TSV `csv`-модулем/`splitlines()` вразрез с `count_part_rows` (единый сплиттер — риск №2); открывать `gdau.duckdb` в `load_day` (conn инъектируется); `mkdir` сверх того, что делают `parquet_store`/`database_manager`.
- [x] **Task 2 — Единый сплиттер строк TSV в `row_check` (риск №2, defer 2.3 `deferred-work.md:35`) — поведение-сохраняющий рефактор**
  - [x] В `scripts/utils/row_check.py` вынести публичный `split_tsv_rows(part: bytes | str) -> list[str]`: декод UTF-8 для `bytes`; `text.split("\n")`; `rstrip("\r")`; отбрасывание пустых — **РОВНО** текущая логика `count_part_rows.py:71–76` (НЕ `splitlines`). Добавить в `__all__`. Переписать `count_part_rows` через него: `return max(0, len(split_tsv_rows(part)) - 1)` — **поведение неизменно** (регресс-тесты 2.3 обязаны остаться зелёными). Обновить docstring `count_part_rows`/модуля: сплиттер теперь общий шов с p81 (2.7), контракт строка↔запись закрыт единым кодом. `count_source_rows`/`verify_row_count`/`RowCountMismatchError` — без изменений сигнатур.
  - [x] Регрессия: существующие `tests/test_row_check.py` зелёные без правок ожиданий (рефактор не меняет числа). При желании — прямой тест `split_tsv_rows` (CRLF/хвостовой перевод/пустые строки → ожидаемый список без заголовочной логики).
- [x] **Task 3 — Спека компонента `docs/ingestion.md` (часть DoD)**
  - [x] **Дополнить** `docs/ingestion.md` разделом **«оркестратор приёма за день»** человеческим языком, без жаргона/сигнатур (он сам в «Границах» обещает: «сборка дня из кусков и весь цикл приёма за день целиком — оркестратор приёма, история 2.7»). Три вопроса: **(1) Что делает** — за один день одного источника проходит весь путь: заказывает выгрузку у Метрики, **дожидается** готовности, **скачивает** все куски, **собирает** из них строки дня, **записывает** одним целым файлом, **сверяет** число строк, **отмечает** день загруженным и **убирает** выгрузку на стороне Метрики (освобождает квоту); всё это — **под замком одного пишущего**; **(2) Зачем** — связать готовые по отдельности шаги в **правильном порядке** так, чтобы день доехал **целиком или никак**: если хоть один кусок не скачался — день **не** записывается вовсе; день считается загруженным **только** после того, как файл на месте и сверка сошлась; уборка выгрузки — уже **после** этого, и её сбой день не отменяет; **(3) Контракт** — на вход дата и источник (`visits`/`hits`), оба грузятся **независимо**; не тот день (будущее) — понятная остановка; ошибка/отмена на стороне Метрики или зависшее ожидание — понятная остановка (не вечное ожидание), а не тихий пропуск; расхождение числа строк — **жёсткая остановка**; повтор того же дня — это просто перезапись одного файла (другие дни не трогаются). **Явно границы:** запись файла — соседний шаг (2.2), сверка — (2.3), журнал — (2.4), замок — (2.5), типизированные представления — (2.6); **решение «какие дни грузить», пропуск загруженных и перезалив свежего окна — следующая история (2.8)**; команда `update` одной строкой — (2.9); разговор с Метрикой по сети — `metrica-client.md`. Не дублировать соседние разделы.
- [x] **Task 4 — Offline-тесты `tests/test_p81_orchestrator.py` (AC: #1–#7, без сети)**
  - [x] `from __future__ import annotations`; **импорт p81 через `importlib.import_module("scripts.8x_metrica_logs_api.p81_load_logs")`** (digit-префикс — риск №3; зафиксировать как образец для 2.9). **Без сети, без реального API:** клиент — **поддельный** (мини-класс/`unittest.mock`), отдающий каноничные ответы (`create`→`{"log_request":{"request_id":1,"status":"created"}}`; `get_log_request`→последовательность статусов до `processed` с `parts`; `download_log_request_part`→`bytes` мини-TSV; `clean`→ok). `conn` — транзиентный `duckdb.connect()` (in-memory) ИЛИ прод-путь через `monkeypatch.setenv(DATA_ROOT_ENV, str(tmp_path))`. **poll без реальных пауз:** `sleep`-шов как no-op (`lambda *_: None`) ИЛИ `poll_interval_s=0`. Каталог — мини-`Catalog` инъекцией (как `test_parquet_store.py`). Кросс-платформенно (`tmp_path`/`pathlib`; CI ubuntu+windows).
  - [x] **AC #1 (happy-path полного цикла):** `load_day(conn, fake_client, "visits", вчера, catalog=mini)` → партиция `data/raw/visits/{date}.parquet` создана (N строк), строка `load_state` `status='loaded'` `row_count=N`, `clean` вызван **после** `mark_loaded` (проверить порядок — напр. фейк фиксирует call-order), возврат == N. `download_log_request_part` вызван по каждой части.
  - [x] **AC #2 (processing_failed/canceled → fail):** фейк `get_log_request`→`status='processing_failed'` (и отдельно `'canceled'`) → `load_day` бросает `RuntimeError` с диагностикой; партиция **не** создана; `load_state` не `loaded` (после реконсиляции/`mark_failed`).
  - [x] **AC #3 (под локом):** `ingest_day("visits", вчера, …)` под занятым локом (предварительно взять `writer_lock(lock_path=tmp/...)` ИЛИ monkeypatch `writer_lock`→raise `WriterLockHeldError`) → fail-fast без записи; в happy-path `ingest_day` — лок взят/освобождён (проверить через шов). Допустимо monkeypatch `DatabaseManager.connection`/`read_metrica_credentials`/`create_views` для изоляции run-level.
  - [x] **AC #4 (оба источника независимо):** `load_day` для `visits` и для `hits` (мини-каталог с обоими) → две независимые партиции + две строки `load_state`; пустой/упавший один источник не влияет на другой (грузятся раздельными вызовами).
  - [x] **AC #5 (poll bounded):** (a) фейк всегда `'processing'` + `poll_timeout_s` крошечный (напр. `0.01`) + `sleep`=no-op → `RuntimeError` «таймаут» (не вечный цикл); (b) `get_log_request` всегда бросает `RuntimeError` → после `max_consecutive_errors` → `RuntimeError` «лимит ошибок»; (c) счётчик подряд-ошибок **сбрасывается** на успешном опросе.
  - [x] **AC #6 (частичные части → не персистится):** фейк: часть 1 ок, `download_log_request_part(req,2)`→`RuntimeError` → `load_day` бросает **до** записи; партиция **не** создана (никаких `.tsv`/`.parquet`/`.tmp` мусора); `load_state` не `loaded`.
  - [x] **AC #7 (clean после коммита):** happy-path, но `clean_log_request`→`RuntimeError` → `load_day` **НЕ** падает, возвращает N, день **остаётся** `loaded` (партиция + `load_state` целы); зафиксировано WARNING (`caplog`).
  - [x] **Сверка-fail (интеграция 2.3):** фейк отдаёт TSV с N строками, но мок `write_partition` (или подделанные данные) даёт actual≠expected → `RowCountMismatchError`; день **не** `loaded` (`mark_loaded` не вызван). Демонстрирует гейт 2.3 в реальном цикле.
  - [x] **Контракт сплиттера (риск №2, defer 2.3):** часть со встроенным `\r`/CRLF/хвостовым переводом строки/пустой строкой → число распарсенных p81 data-строк **==** `count_part_rows(part)` (тот же `split_tsv_rows`). Доказывает отсутствие off-by-N misfire.
  - [x] **Пустой день (риск №8):** фейк `processed` с `parts=[]` → `columns=catalog.metrica_fields(source)`, `rows=[]` → пустая типизированная партиция, `verify_row_count(0,0)` ок, `mark_loaded(...,0)`; день `loaded`.
  - [x] **Граница дат (риск №10):** `load_day(..., date=завтра)` → `ValueError` (будущий день), без `create_log_request` (фейк не вызван).
  - [x] **Анти-зависимость (через `ast`, приём `test_parquet_store.py:387`):** в `p81_load_logs.py` нет top-level импорта `pandas`/`polars`/`numpy`/`pyarrow`, directaiq-инфры `base_script`/`config_manager`/`view_builders`. `duckdb`/`requests`(косвенно) и все `scripts.utils.*` — разрешены (p81 их и склеивает).
  - [x] **Live-тест НЕ здесь** (он в Task 5). Offline-набор детерминирован, без сети и без реальных пауз.
- [x] **Task 5 — Live-smoke `tests/test_p81_orchestrator_live.py` (`@pytest.mark.live`; ОБЯЗАТЕЛЕН — риск №4)**
  - [x] `from __future__ import annotations`; `import pytest`; функции под `@pytest.mark.live` (отсеяны `addopts="-m 'not live'"`; явный `uv run pytest -m live`). Импорт p81 через `importlib` (риск №3).
  - [x] **Skip-гард:** нет `GDAU_DATA_ROOT`/`.env`/кредов (`read_metrica_credentials` → `ValueError`) → `pytest.skip("нет кредов/хранилища для live-smoke")` (не ложный красный). Креды — из `.env` per-game хранилища.
  - [x] **Реальный цикл:** `ingest_day("visits", вчера_по_МСК)` (узкое окно **1 день**, поля из каталога — прод-путь) против **реального** Logs API: create→poll→download→write→verify→`load_state`→clean. Уважать rate-limit (≤5000/day) и poll (~30s — реальный таймаут оставить дефолтным/умеренным). Ассерт: партиция создана, `load_state` `loaded`, число строк ≥ 0. Опц. второй кейс — `hits`.
  - [x] **Контракт API ↔ каталог:** заголовок реального TSV (родные имена) ⊆/== `catalog.metrica_fields("visits")`; формат TSV (tab-разделитель, заголовок-на-часть) соответствует допущениям парсера; статусы цикла наблюдаемы (`created`/`processing`/`processed`). Расхождение → информативный fail (контракт разошёлся — то, ради чего live существует).
  - [x] **Освежить фикстуры:** добавить `tests/fixtures/logs_hits_sample.tsv` (мини, по образцу `logs_visits_sample.tsv`); при возможности обновить визиты/хиты из реального ответа (формат массивов `[v1,v2]`/`[]`, экранирование строковых массивов — подтвердить предпосылку решения 2.6 вариант A). Зафиксировать в Dev Agent Record, что фикстуры освежены из live (закрытие defer 2.3 `deferred-work.md:35`).
- [x] **Task 6 — Гейты верификации (обязательны перед закрытием)**
  - [x] `uv run mypy scripts` → зелено (strict; `conn: duckdb.DuckDBPyConnection`; `client: MetricaClient`; poll-параметры; без `Any`-дыр; `NoReturn`/типы где нужно).
  - [x] `uv run pytest` → зелено (новый offline `test_p81_orchestrator.py` + регрессия 1.x/2.1–2.6 **включая `test_row_check.py` без правок ожиданий**; live отсеян). На обеих ОС (digit-импорт/`importlib`/пути).
  - [x] `uv run pytest -m live` → задокументировать ручной прогон (или `skip` без кредов) в Dev Agent Record; фикстуры освежены.
  - [x] Новых зависимостей нет (`importlib`/`time`/`logging`/`collections.abc` — stdlib; `duckdb`/`requests` уже в стеке) → **`uv.lock` не меняется**.
  - [x] Прогнать чек-лист «Definition of Done».

## Review Findings

_Code review 2026-05-25 (bmad-code-review; 3 adversarial-слоя на Opus: Blind Hunter / Edge Case Hunter / Acceptance Auditor). Acceptance Auditor с полным контекстом перепрогнал offline-гейты (pytest зелёный, mypy strict 19 файлов) → **все 7 AC PASS**. Ниже — 3 патч-находки; ~9 находок отсеяны как шум/уже-обработанные (бесконечный poll-overshoot на 1 интервал, голый `UnicodeDecodeError`/`KeyError req_id` — ловятся `mark_failed`+re-raise, тавтология `written >= 0` в live при наличии сильной проверки рядом, неиспользуемая фикстура hits — по спеке, расширение типа `_parse_parts | None` — корректнее эскиза, и т.п.)._

- [x] [Review][Patch] Заголовки последующих TSV-частей не сверяются с первым — риск тихого рассогласования колонок [scripts/8x_metrica_logs_api/p81_load_logs.py:380-383] — `_parse_parts` берёт `header` из первой непустой части и отбрасывает `lines[0]` КАЖДОЙ части как заголовок, но не проверяет совпадение заголовков. Если часть придёт с тем же числом колонок, но другим порядком/набором — ячейки лягут под именами part-1, при этом `count_source_rows` и `write_partition` дадут равные счётчики → `verify_row_count` пройдёт → **тихая порча данных закоммитится**. (Разную ширину ловит `write_partition` fail-loud; равная ширина при ином порядке — дыра.) Фикс: сверять `lines[0].split("\t")` каждой непустой части с первым `header`; расхождение → `RuntimeError` ДО `write_partition`. Целостность БД — строже (project-context). Источник: edge (high) + blind.
- [x] [Review][Patch] `clean` после коммита ловит только `(RuntimeError, OSError)` — у́же контракта AC #7 [scripts/8x_metrica_logs_api/p81_load_logs.py:220] — AC #7: сбой `clean` после `mark_loaded` = WARNING, день остаётся загружен, НИКОГДА не fail. Реальный `MetricaClient.make_request` оборачивает всё в `RuntimeError`, поэтому сейчас контракт держится; но любое иное исключение из `clean` после точки коммита пробросится и превратит уже-закоммиченный день в провал вызова. Фикс: расширить до `except Exception` (после коммита единственная операция — сетевой `clean`; любой его сбой → WARNING, не fail). Источник: blind (medium) + edge.
- [x] [Review][Patch] `_download_all_parts`: голый `part["part_number"]` вместо диагностики [scripts/8x_metrica_logs_api/p81_load_logs.py:352] — прямой subscript бросит сырой `KeyError` без `req_id`/контекста части, если part-словарь без ключа (реальный API ключ всегда отдаёт; целостность не страдает — путь до коммита, `mark_failed`+re-raise). Расходится с богатой fail-loud-диагностикой остального модуля и с CLI-братом (`logs_api_cli` использует `.get`). Фикс: извлекать с понятной `RuntimeError` (`req_id` + part). Источник: blind+edge+auditor (low).

**Применено в этой сессии ревью (2026-05-25, прогон против РЕАЛЬНОГО Logs API, счётчик 108178404):**

- [x] [Review][Patch] ×3 — все три патча выше применены; +3 регресс-теста (`test_parse_parts_rejects_divergent_headers`, `test_clean_failure_non_runtime_exception_still_warning`, `test_download_all_parts_missing_part_number_fails_loud`).
- [x] [Review][Perf] **КРИТИЧНО — `parquet_store.write_partition` не масштабировался на реальном объёме** [scripts/utils/parquet_store.py] — построчный `conn.executemany(INSERT … VALUES)` в колоночный DuckDB вис **25+ минут** на реальном дне hits (**79 472 строки / 41 колонка / ~54 МБ TSV**). Offline-моки в 3 строки этот путь не нагружали — вскрыто live-smoke 2.7 (ровно его цель). **Фикс:** bulk-загрузка через транзиентный CSV + нативный `read_csv` (без pandas/pyarrow); round-trip None↔NULL / пустая строка / спецсимволы подтверждён эмпирически (`allow_quoted_nulls=false`, `all_varchar=true`); +тест `test_special_characters_round_trip_verbatim`. **Итог: hits 25+ мин (зависание) → PASSED 65s.** Баг принадлежит истории 2.2, но без него цель 2.7 (день реально доезжает от API до сырья) не достигалась — поэтому исправлен в этой сессии.
- [x] [Review][Catalog] **Сверка visits-каталога с официальным справочником Logs API — два дефекта исправлены:** (1) убрано Pro-only `ym:s:isRobotPro` (тариф счётчика не выгружает); (2) **19 полей маркетинга/атрибуции** (источник трафика, Директ, соцсети, рекомендации, мессенджеры) были записаны БЕЗ обязательного префикса модели атрибуции → Logs API отклонял их как `Unknown field`. Формат справочника — `ym:s:<attribution>TrafficSource` (`logs/param`). Возвращён префикс `last` (зеркало уже-корректной hits-части каталога): `ym:s:TrafficSource`→`ym:s:lastTrafficSource`, storage `traffic_source`→`last_traffic_source` и т.д. Снимок-счётчик `test_catalog.py`: visits 74→73 (минус isRobotPro), всего 115→114; 19 переименований счётчик не меняют.

**Открытые follow-up (НЕ код 2.7; занесены в `deferred-work.md`):**

- [x] [Review][Defer] **✅ РЕШЕНО** — visits live был красным из-за дефекта каталога (потерянный префикс атрибуции у 19 полей, НЕ тариф). После фикса каталога **visits live PASSED 80s (1893 строки × 73 колонки)**. Оба источника зелёные.
- [ ] [Review][Defer] Освежить `tests/fixtures/logs_hits_sample.tsv` из реального hits-ответа (defer 2.3(b): подтвердить формат массивов/экранирование живьём). Реальный контракт уже косвенно подтверждён live (hits 79 472 строки, visits 1893 строки, колонки партиций ⊆ каталога), но сама фикстура остаётся рукописной. Единственный оставшийся follow-up.

**Верификация после всех изменений (2026-05-25):** `uv run pytest` → **296 passed / 5 deselected**; `uv run mypy scripts` (win32 + `--platform linux`) → **Success, 19 файлов**; live **hits PASSED 65s** + **visits PASSED 80s** (оба источника, полный реальный цикл с perf-фиксом + 3 патчами + фиксом каталога). `uv.lock` не менялся.

## Dev Notes

### Рекомендуемый контракт p81 (вариант A; финализируй под 2.8/2.9)

| Имя | Сигнатура | Смысл |
|---|---|---|
| `load_day` | `(conn: duckdb.DuckDBPyConnection, client: MetricaClient, source: str, date: str, *, catalog: Catalog \| None = None, poll_interval_s: float = POLL_INTERVAL_S, poll_timeout_s: float = POLL_TIMEOUT_S, max_consecutive_errors: int = MAX_CONSECUTIVE_POLL_ERRORS, sleep: Callable[[float], None] = time.sleep) -> int` | **ядро цикла дня** на инъектируемых `conn`+`client` (тестируемый шов): mark_loading→create→poll→download-всё→parse→write→verify→**mark_loaded**→clean; возврат — число строк |
| `ingest_day` | `(source: str, date: str, *, catalog: Catalog \| None = None, …poll-параметры…) -> int` | **run-level одного дня**: `writer_lock` + write-`conn` + клиент + `ensure_load_state_table` + `create_views` (один раз) → `load_day`. **2.8 не зовёт в цикле** (реентрантность лока) |
| `_poll_until_processed` | `(client, req_id, *, …) -> dict` | bounded poll (интервал/таймаут/лимит ошибок); `processed`→info; `canceled`/`processing_failed`→RuntimeError (AC #2) |
| `_download_all_parts` | `(client, req_id, info) -> list[bytes]` | всё-или-ничего в память (AC #6) |
| `_parse_parts` | `(parts_bytes) -> tuple[list[str], list[list[str]]]` | единый `split_tsv_rows` (риск №2); (header, data-rows) |

**Использование (вариант A; 2.8 берёт лок ОДИН раз вокруг диапазона):**
```python
# 2.7 — ad-hoc один день:
ingest_day("visits", "2026-05-24")           # лок+conn+клиент внутри, для одного дня

# 2.8 (будущая) — диапазон под ОДНИМ локом (НЕ через ingest_day — реентрантность!):
with writer_lock():                          # один захват на весь прогон (2.5/риск №1)
    with DatabaseManager.connection() as conn:
        client = MetricaClient(token=…, counter_id=…)
        ensure_load_state_table(conn)
        create_views(conn)
        loaded = reconcile(conn)             # 2.4 — что уже загружено
        for source, date in days_to_load:    # 2.8 решает набор (skip+hot-window)
            load_day(conn, client, source, date)
```

### Порядок операций дня (протокол идемпотентного дня; architecture.md:379–381, риск №5)

```
mark_loading(conn, source, date)                 # 2.4 — день «в процессе» (реконсиляция: незагружен)
  → fields = catalog.metrica_fields(source)       # 1.5 — список из SSOT (FR-2)
  → resp = client.create_log_request(date,date,fields,source)  # 1.3
  → req_id = (resp.get("log_request") or resp)["request_id"]
  → info = poll до status=='processed'            # риск №5/№7; failed/canceled → RuntimeError (AC #2)
  → parts_bytes = download ВСЕХ частей в память    # AC #6 всё-или-ничего (риск №6)
  → columns, rows = parse(parts_bytes)             # единый split_tsv_rows (риск №2); 0 частей → catalog-поля,[] (риск №8)
  → expected = count_source_rows(parts_bytes)      # 2.3 — сырой счёт (заголовок на часть)
  → actual   = write_partition(source, date, columns, rows)  # 2.2 — temp→os.replace атомарно
  → verify_row_count(expected, actual, source=, date=)       # 2.3 — расхождение → RowCountMismatchError (НЕ глушить)
  → mark_loaded(conn, source, date, actual)        # 2.4 === ТОЧКА КОММИТА ДНЯ
  → clean_log_request(req_id)                       # 1.3 — ПОСЛЕ коммита; fail → WARNING, день остаётся (AC #7)
# на любом исключении до коммита: mark_failed(best-effort) + re-raise (риск №12)
```

### Карта примитивов (что зовём; точные сигнатуры — по факту кода)

- `MetricaClient.create_log_request(date1, date2, fields, source, attribution=...)` → **полный** ответ (`metrica_client.py:228`); `get_log_request(request_id) -> dict` уже отдаёт **внутренний** `log_request` (`:284–296`, есть `status`/`parts`); `download_log_request_part(request_id, part_number) -> bytes` (`:298`); `clean_log_request(request_id)` (`:322`). Retry/rate-limit/бэкофф **внутри** клиента (`:101–171`) — заново НЕ реализуем (NFR-3, AC #5).
- `dates.moscow_yesterday()` (потолок/якорь, `dates.py:58`), `parse_date`/`format_date` (строгий `YYYY-MM-DD`, `:63/:85`). Одиночный день — **валидировать** ≤ вчера, НЕ `clamp_date_range` (риск №10; диапазонный clamp — 2.8).
- `catalog.load_catalog().metrica_fields(source) -> list[str]` (`catalog.py:166` — список выгрузки из SSOT, FR-2); `VALID_SOURCES` (`:35`).
- `parquet_store.write_partition(source, date, columns, rows, *, catalog=None) -> int` (`parquet_store.py:48`): `columns` = **родные имена** в порядке заголовка (он мапит→snake_case, неизвестная→`ValueError`); `rows` = `Iterable[Sequence[str|None]]`; возврат — число строк (для сверки). Пустой `rows` (`[]`) = валидный пустой день.
- `row_check.count_source_rows(parts: Iterable[bytes|str]) -> int` (`row_check.py:81` — передавать **итерируемое частей**, не одну `bytes`!); `verify_row_count(expected, actual, *, source, date)` → `RowCountMismatchError` при расхождении (`:104`). **+ новый** `split_tsv_rows` (Task 2).
- `load_state.ensure_load_state_table(conn)` (`load_state.py:107`), `mark_loading`/`mark_loaded(...,row_count)`/`mark_failed` (`:116/:137/:149`), `reconcile(conn)` (`:192` — для 2.8). Все на инъектируемом `conn`.
- `writer_lock(*, lock_path=None)` контекст-менеджер (`writer_lock.py:71`; `WriterLockHeldError` при занятом — `:63`; **не реентерабелен** — риск №1). `DatabaseManager.connection(read_only=False)` (`database_manager.py:39`; write-conn создаёт файл/каталог). `views.create_views(conn, *, catalog=None, sources=VALID_SOURCES)` (2.6, `ready-for-dev`). `env_reader.read_metrica_credentials() -> MetricaCredentials(token, counter_id)` (`env_reader.py:48`; fail-loud до сети).

### Паттерны от историй 1.x/2.1–2.6 (соблюдать — снижают цикл ревью)

- `from __future__ import annotations` первой строкой; русский модульный docstring (роль + границы + «склейка, не повтор»); идентификаторы английские, docstrings/комментарии русские.
- Type hints везде, `mypy --strict` по `scripts`, без `Any`-дыр. Абсолютные импорты от корня пакета. `logger = logging.getLogger(__name__)` напрямую (НЕ `logging_utils`). CLI/`print` — не здесь (поверхность 2.9); диагностика — `logging`.
- **`conn`/`client` инъектируются в `load_day`** (приём `load_state` 2.4/`views` 2.6: ядро БД/сети сам не открывает; `database_manager`/`MetricaClient`-конструктор — забота run-level `ingest_day`/2.8). `catalog=None`→`load_catalog()` (шов как `parquet_store`).
- **Тонкий run-level + тестируемое ядро** (приём 2.6 `build_view_ddl`/`create_views`): `load_day` без лока/сети-обвязки тестируется поддельным клиентом + in-memory conn; `ingest_day` исполняет с реальными лок/conn/клиент.
- Fail-loud наследуется: битый корень/нет кредов → `ValueError` ДО сети (paths/env_reader); `source` — `VALID_SOURCES`; сверка — `RowCountMismatchError` наружу.
- Анти-зависимость через `ast` (import-узлы; приём `test_parquet_store.py:387`); `duckdb`/`scripts.utils.*` разрешены, directaiq-инфра/аналит-стек — нет.
- **Live-набор ОБЯЗАТЕЛЕН** (в отличие от 2.1–2.6): p81 — внешний API. Маркер `@pytest.mark.live`, skip без кредов; фикстуры освежить (риск №4, [[realapi-smoke-tests]]).

### Границы 2.7 (не выходить)

- Новый модуль `scripts/8x_metrica_logs_api/p81_load_logs.py` (+ поведение-сохраняющий рефактор `row_check.split_tsv_rows` Task 2 + дополнение `docs/ingestion.md` + `tests/test_p81_orchestrator.py` + `tests/test_p81_orchestrator_live.py` + фикстура `logs_hits_sample.tsv`). **Не** реализуем: запись Parquet (2.2), сверку (2.3), мету/реконсиляцию (2.4), лок (2.5), view (2.6), клиент/даты/каталог/креды (1.x) — **зовём готовое**.
- **Решение «какие дни грузить», пропуск загруженных, идемпотентный перезалив окна, hot-window N=3, диапазонный clamp — 2.8.** 2.7 грузит **переданный** день (идемпотентно через `write_partition` 2.2). **Команда `gdau-logs update` (поверхность, exit-коды, агрегация источников) — 2.9.** MCP-чтение — 3.1.
- `load_day` не берёт лок и не открывает БД (инъекция); лок-scope — зацикливающий вход (риск №1). Не тащить инфру directaiq (`BaseScript`/`config_manager`/параллель/`DROP TABLE`/`view_builders`).
- Конкуренция читатель↔писатель на Windows `os.replace` (defer 2.2 `deferred-work.md:31`) — рантайм-забота 3.1, не 2.7 (читателей пока нет).

### Project Structure Notes

- Модуль — `scripts/8x_metrica_logs_api/p81_load_logs.py` ровно по дереву архитектуры (architecture.md:458, «ОРКЕСТРАТОР: create→poll→download→parquet→сверка→meta»). Каталог уже есть (`.gitkeep`), **без `__init__.py`** (неявный namespace-пакет) → импорт через `importlib.import_module` (риск №3). Имена snake_case; type hints обязательны (mypy strict).
- Не переводить на src-layout, не переименовывать пакет `scripts` (ломает резолюцию — hatchling `packages=["scripts"]`).
- `tests/` зеркалит `scripts/`: `tests/test_p81_orchestrator.py` (имя — architecture.md:484) + live `tests/test_p81_orchestrator_live.py` (конвенция `*_live.py`, как `test_metrica_client_live.py`). Конфиг pytest (`markers`/`addopts`) есть (1.3); `conftest.py`/`tests/__init__.py` нет — `tmp_path`/`monkeypatch`/`importlib` напрямую.
- `docs/ingestion.md` — **дополняется** (Task 3): уже описывает 2.2–2.5 и обещает «цикл приёма за день — история 2.7». Часть DoD (project-context: компонент без актуальной спеки не «готов»).
- `gdau.duckdb`/`*.parquet`/`.writer.lock`/`*.tsv` — артефакты хранилища (под `GDAU_DATA_ROOT`), в dev-репо не создаются и не коммитятся (`.gitignore`). Offline-тесты пишут только в `tmp_path`.
- `uv.lock` не трогаем — всё в стеке/stdlib. Не реорганизовывать раскладку.

### Definition of Done — чек-лист самопроверки

1. `scripts/8x_metrica_logs_api/p81_load_logs.py`: ядро `load_day(conn, client, …)` (инъекция, без лока/открытия БД/сети-retry) + run-level `ingest_day` (лок+conn+клиент+ensure_table+create_views один раз; **вариант A — УТВЕРЖДЁН Шефом**, риск №1). Полный цикл create→poll→download→parse→write→verify→mark_loaded→clean. (AC #1)
2. `processing_failed`/`canceled` при poll → `RuntimeError` с диагностикой, без молчаливого пропуска; день не `loaded`. (AC #2)
3. Любая запись — под `.writer.lock` (run-level `ingest_day`/2.8); `load_day` лок не берёт; лок не реентерабелен. (AC #3, риск №1)
4. Оба источника visits+hits грузятся независимо (тот же `load_day`, `source∈VALID_SOURCES`); связь `watch_ids↔watch_id` — факт каталога, не спецкод. (AC #4)
5. Poll ограничен: интервал ~30s, верхняя граница (`poll_timeout_s`), лимит подряд-ошибок (`max_consecutive_errors`) → fail; сброс счётчика на успехе; rate-limit/retry — из клиента; сон — инъектируемый шов (тесты без реальных пауз). (AC #5, риск №7)
6. Частичные части → исключение **до** `write_partition`, день не персистится, нет мусора (части в памяти). (AC #6, риск №6)
7. Точка коммита = `mark_loaded` (после rename+сверки); `clean` после коммита, его fail → WARNING, день остаётся загружен; fail до коммита → `mark_failed`(best-effort)+raise. (AC #7, риск №5/№12)
8. Единый сплиттер `row_check.split_tsv_rows` (Task 2, поведение-сохраняющий рефактор; `count_part_rows` через него, регресс 2.3 зелёный); p81 парсит TSV тем же сплиттером; контракт-тест на встроенный перевод строки. (риск №2, defer 2.3)
9. Пустой день (0 частей) → колонки из каталога, `rows=[]`, `verify(0,0)` ок, день `loaded`. Граница дат: будущий день → fail-loud, не молчаливый clamp. (риск №8/№10)
10. `docs/ingestion.md` дополнен разделом «оркестратор приёма за день» (3 вопроса; границы 2.2/2.3/2.4/2.5/2.6/2.8/2.9/3.1 названы) — DoD компонента. (Task 3)
11. Offline `test_p81_orchestrator.py` (AC #1–#7 + сверка-fail + сплиттер-контракт + пустой день + граница дат + ast-анти-зависимость; поддельный клиент, in-memory conn, sleep-no-op, импорт через `importlib`). (Task 4)
12. **Live `test_p81_orchestrator_live.py` (`@pytest.mark.live`, ОБЯЗАТЕЛЕН): реальный цикл 1 день, skip без кредов, контракт API↔каталог; фикстуры освежены (`logs_hits_sample.tsv` добавлен).** Закрывает defer 2.3 (формат TSV/массивов). (Task 5, риск №4)
13. `uv run mypy scripts` и `uv run pytest` — зелёные на обеих ОС; `test_row_check.py` зелёный без правок ожиданий; `uv.lock` не менялся; `data/`-артефактов в dev-репо не создано.
14. Велась в отдельной ветке `story/2.7-orchestrator` (новая история → новая ветка); merge в `main` только после зелёного CI на обеих ОС. PR в `main`.

### Latest Tech Information

- **Веб-ресёрч не требуется:** стек зафиксирован локом; `requests`/`duckdb`/`mcp` стабильны; внешний контракт — Logs API (Яндекс), он подтверждается **live-smoke**, а не вебом. Реальный wire-формат TSV/массивов/статусов цикла — за `test_p81_orchestrator_live.py` (риск №4).
- **`importlib.import_module` для digit-префикс-пакета (проверено логикой defer 1.1):** `import_module("scripts.8x_metrica_logs_api.p81_load_logs")` работает (строка, не идентификатор); `import scripts.8x_…` как statement = `SyntaxError`. Каталог без `__init__.py` = неявный namespace-пакет — `import_module` отрабатывает. Это же — образец для импорта p81 в CLI `update` (2.9).
- **Logs API цикл (по коду клиента 1.3 + directaiq-референс):** `create_log_request` (POST) → `get_log_request` (GET, polling; внутренний dict с `status`/`parts`) → `download_log_request_part` (GET, `bytes`) → `clean_log_request` (POST). Статусы: `created`/`processing` (ждать), `processed` (готово), `canceled`/`processing_failed` (ошибка). Заголовок TSV — в каждой части (off-by-P-гард `row_check`); разделитель `\t`. **Допущения подтверждаются live-smoke.**
- **Сверка как жёсткий fail (осознанное отличие от directaiq):** directaiq `p81_load_logs.py:783` на расхождении CSV↔импорт пишет **warning** и продолжает — у нас `verify_row_count` бросает `RowCountMismatchError` (наружу non-zero). НЕ копировать warning-поведение вендора.
- **`os.replace` атомарность дня** уже в `parquet_store` (2.2) — p81 её НЕ дублирует; перезалив того же дня = вызов `write_partition` (один файл), без `DROP` (анти-паттерн directaiq `_force_drop_tables`).

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 2.7] (строки 324–338) — user story + 7 AC (включая edge: частичные parts, clean после коммита).
- [Source: _bmad-output/planning-artifacts/epics.md#FR-1] (строка 23) — оркестрация полного цикла; fail на `processing_failed`/`canceled`; rate-limit/retry наследуется от клиента.
- [Source: _bmad-output/planning-artifacts/epics.md#FR-3] (строка 25) — оба источника независимо; связь `visits.watchIDs`↔`hits.watchID`.
- [Source: _bmad-output/planning-artifacts/epics.md#Epic 2] (строки 230–232) — место 2.7 в цепочке 2.1→2.9 (2.8 инкремент/hot-window, 2.9 `update`).
- [Source: _bmad-output/planning-artifacts/epics.md#Story 2.8/2.9] (строки 340–369) — границы: «какие дни/skip/hot-window» = 2.8; поверхность `update`/exit-коды/агрегация = 2.9 (НЕ 2.7).
- [Source: _bmad-output/planning-artifacts/architecture.md#Communication & Process] (строки 379–387) — протокол идемпотентного дня (download→.tmp→сверка→rename→load_state); poll ~30s/верхняя граница/лимит ошибок; лок один раз; сверка = fail не warning.
- [Source: _bmad-output/planning-artifacts/architecture.md#Integration Points] (строки 536–538) — поток приёма (write): `update`→p81 берёт лок→клиент→`parquet_store` .tmp→сверка→rename→`load_state`.
- [Source: _bmad-output/planning-artifacts/architecture.md#Directory Structure] (строки 458, 484, 524, 529) — `p81_load_logs.py` «своя оркестрация»; `test_p81_orchestrator.py`; FR-1/3/11–15 → p81.
- [Source: _bmad-output/project-context.md#Data & Domain] (строки 105–117) — протокол идемпотентного дня; сверка = жёсткий fail; один писатель; реконсиляция; единственная точка HTTP — `metrica_client`; retry/rate-limit только оттуда; цикл create→poll→download→clean; `date2` clamp.
- [Source: _bmad-output/project-context.md#Testing] (строки 139–146) — live-smoke против реального Logs API обязателен (моки расходятся с реальностью); маркер `@pytest.mark.live`; skip без кредов; фикстуры освежать из live-прогона.
- [Source: scripts/utils/metrica_client.py:228–345] — `create_log_request`/`get_log_request`/`download_log_request_part`/`clean_log_request`; retry/rate-limit внутри (`:101–171`) — заново не реализуем.
- [Source: scripts/utils/parquet_store.py:48–69] — `write_partition(source, date, columns, rows, *, catalog) -> int`; `columns`=родные имена в порядке заголовка; пустой `rows`=валидный пустой день.
- [Source: scripts/utils/row_check.py:59–101, 104–135] — `count_part_rows` (сплит `\n`/`rstrip("\r")`, заголовок на часть — источник `split_tsv_rows`), `count_source_rows` (итерируемое частей!), `verify_row_count`→`RowCountMismatchError`.
- [Source: scripts/utils/load_state.py:107–157, 192–215] — `ensure_load_state_table`/`mark_loading`/`mark_loaded`/`mark_failed`/`reconcile`; `conn` инъектируется; `loading`/`failed`=незагружен.
- [Source: scripts/utils/writer_lock.py:71–95, 82] — `writer_lock(*, lock_path=None)`; `WriterLockHeldError`; **не реентерабелен** «один захват на весь прогон — p81 (2.7)» (риск №1).
- [Source: scripts/utils/database_manager.py:39–60] — `DatabaseManager.connection(read_only=False)` write-conn для p81/init.
- [Source: scripts/utils/dates.py:58–116] — `moscow_yesterday`/`parse_date`/`format_date`/`clamp_date_range` (диапазонный clamp — для 2.8, не одиночного дня — риск №10).
- [Source: scripts/utils/catalog.py:166–176] — `metrica_fields(source)` (список выгрузки FR-2), `duckdb_types`, `VALID_SOURCES`.
- [Source: scripts/utils/env_reader.py:48–59] — `read_metrica_credentials() -> MetricaCredentials(token, counter_id)` fail-loud до сети.
- [Source: scripts/tools/logs_api_cli.py:176–205, 233–256, 284–350] — образец вызова клиента (defensive `resp.get("log_request") or resp`; статус-гейт `processed`; `size`-коэрция); **граница**: CLI — ad-hoc примитивы, p81-приём — 2.7/2.9.
- [Source: scripts/8x_metrica_logs_api/.gitkeep] — каталог без `__init__.py` (неявный namespace; `import_module` — риск №3).
- [Source: directaiq scripts/8x_metrica_logs_api/p81_load_logs.py:466–546, 590–692, 783] — референс цикла (create→wait→download→import→clean); **анти-образцы НЕ копировать**: `BaseScript`, `config_manager`/`view_builders`, `_force_drop_tables`(DROP), параллельная очередь, сверка как **warning** (`:783`).
- [Source: _bmad-output/implementation-artifacts/deferred-work.md:6] — defer 1.1: digit-префикс пакета `8x_metrica_logs_api`, `importlib.import_module` (риск №3).
- [Source: _bmad-output/implementation-artifacts/deferred-work.md:35] — defer 2.3: единый сплиттер записей p81↔`count_part_rows` + контракт-тест на встроенный перевод строки; формат TSV подтверждает live-smoke 2.7 (риск №2/№4).
- [Source: _bmad-output/implementation-artifacts/deferred-work.md:31] — defer 2.2: Windows `os.replace` под читателем — забота 3.1, не 2.7.
- [Source: _bmad-output/implementation-artifacts/deferred-work.md:27] — defer 1.6: частичные `.tsv` при сбое `download` — p81 держит части в памяти (риск №6).
- [Source: _bmad-output/implementation-artifacts/2-6-…try-cast.md:30, 67] — решение 2.6 вариант A (формат массива `[v1,v2]`); экранирование строковых массивов подтверждает live-smoke 2.7.
- [Source: docs/ingestion.md:205–231] — «Границы»: сборка дня и весь цикл приёма — обещаны истории 2.7 (Task 3).
- [Source: tests/fixtures/logs_visits_sample.tsv] — реальный мини-TSV формат (`ym:s:visitID\t…`, массив `[8273645,8273646]`); добавить `logs_hits_sample.tsv` (Task 5).
- [Source: tests/test_parquet_store.py:387] — паттерн ast-анти-зависимости (import-узлы) — зеркало для `test_p81_orchestrator.py`.
- [Memory: realapi-smoke-tests] — live-smoke обязателен для внешнего API; 2.7 — первый после 1.6, закрывает defer 2.3. [[simplicity-first]] — `importlib` вместо `spec_from_file_location`; poll-константы вместо `config_manager`; единый сплиттер вместо дублирования. [[structure-mirror-directaiq]] — форма p81 узнаваема, но инфра directaiq не тащится. [[field-scope-decisions]] — поля выгрузки = весь каталог (FR-2). [[review-mode-edge-case-hunter]] — Шеф гоняет edge-case hunter: риски №2 (сплиттер), №5 (порядок/коммит), №6 (частичные части), №7 (poll), №8 (пустой день), №10 (clamp дня) разобраны проактивно.

## Dev Agent Record

### Agent Model Used

claude-opus-4-7[1m] (Opus 4.7, 1M context) — dev-story workflow.

### Debug Log References

- `uv run pytest tests/test_row_check.py` → 27 passed (23 регресс + 4 новых split-теста; поведение `count_part_rows` неизменно).
- `uv run pytest tests/test_p81_orchestrator.py` → 18 passed (AC #1–#7 + сверка-fail + сплиттер-контракт + пустой день + граница дат + невалидный источник + ast-анти-зависимость).
- `uv run pytest -q` → 292 passed, 5 deselected (полная регрессия 1.x/2.1–2.6 зелёная, включая `test_row_check.py` без правок ожиданий).
- `uv run mypy scripts` (win32) и `uv run mypy scripts --platform linux` → Success, 19 source files (без `Any`-дыр).
- `uv run pytest -m live tests/test_p81_orchestrator_live.py` → 2 skipped (нет кредов/`GDAU_DATA_ROOT` в dev-окружении — корректный skip, не ложный красный).
- **РЕАЛЬНЫЙ live-прогон выполнен 2026-05-25** против счётчика `108178404` (креды в `.env` хранилища). Результат и находка — в подсекции «Live-прогон против реального Logs API» ниже. Кратко: `visits` падает на `create` (400 — тариф не даёт поле `ym:s:isRobotPro`), `hits` проходит. **Это расхождение каталог↔тариф, не дефект оркестратора** (он отработал по AC #2: fail-loud + `mark_failed`).
- `git diff --name-only` → `uv.lock` не менялся.

### Completion Notes List

- **Вариант A (утверждён Шефом):** ядро `load_day(conn, client, …)` (инъекция; не берёт лок, не открывает БД, не строит клиент — главный тестируемый шов) + run-level `ingest_day(source, date)` (один захват `.writer.lock`, write-conn, клиент из кредов, `ensure_load_state_table`+`create_views` один раз → `load_day`). В docstring `ingest_day` зафиксировано: 2.8 берёт лок один раз вокруг диапазона и зовёт `load_day` напрямую (НЕ `ingest_day` в цикле — `writer_lock` не реентерабелен).
- **Протокол идемпотентного дня (AC #1, #5, #7):** `mark_loading` → `create` → bounded poll → download всех частей в память → парсинг TSV → `write_partition` → `verify_row_count` → `mark_loaded` (**точка коммита**) → `clean`. `clean` после коммита, его сбой → WARNING, день остаётся загружен (AC #7). Весь блок до коммита в `try/except BaseException` → `mark_failed` best-effort (`contextlib.suppress`) + re-raise (AC #2, риск №12).
- **Единый сплиттер (Task 2, defer 2.3):** `row_check.split_tsv_rows(part)` — общий шов границ строк; `count_part_rows` переписан через него (`max(0, len(split_tsv_rows(part)) - 1)`, поведение неизменно — регресс зелёный). p81 парсит TSV тем же сплиттером; контракт-тест доказывает `len(распарсенных p81 строк) == count_part_rows(part)` на CRLF/пустых строках → off-by-N misfire исключён.
- **Всё-или-ничего (AC #6):** `_download_all_parts` качает ВСЕ части в память; любая упала → исключение ДО `write_partition`, день не персистится (ни `.parquet`, ни `.tmp` мусора — части в памяти, не россыпью `.tsv`).
- **Bounded poll (AC #5):** модульные константы `POLL_INTERVAL_S=30.0`/`POLL_TIMEOUT_S=3600.0`/`MAX_CONSECUTIVE_POLL_ERRORS=5` + переопределяемые kwargs (`config_manager` не тащим, NFR-6); сон через инъектируемый `sleep` (тесты — no-op). Таймаут по `time.monotonic`, лимит подряд-ошибок, сброс счётчика на успешном опросе; `canceled`/`processing_failed` → fail с диагностикой (AC #2).
- **Пустой день (риск №8):** 0 частей / части без строк → колонки из каталога, `rows=[]` → пустая типизированная партиция, `verify(0,0)` ок, день `loaded`. **Граница дат (риск №10):** будущий день (> вчера по МСК) → fail-loud `ValueError` ДО `create`, не молчаливый clamp.
- **digit-префикс (риск №3):** модуль `scripts/8x_metrica_logs_api/p81_load_logs.py` (каталог без `__init__.py` = namespace-пакет); тесты и live импортируют через `importlib.import_module("scripts.8x_metrica_logs_api.p81_load_logs")` — образец для CLI `update` 2.9. Прямого entry-point у p81 нет.
- **Своя оркестрация (NFR-6):** ast-тест фиксирует отсутствие `pandas`/`polars`/`numpy`/`pyarrow` и directaiq-инфры (`base_script`/`config_manager`/`view_builders`); `duckdb`/`scripts.utils.*` разрешены. Сверка — жёсткий `RowCountMismatchError` наружу (НЕ warning-вендор).
- **Фикстуры:** добавлен `tests/fixtures/logs_hits_sample.tsv` (заголовок `ym:pv:*` + массив `goalsID`, в т.ч. пустой `[]`). Освежение из реального ответа — за ручным live-прогоном Шефа (закрытие defer 2.3: реальный wire-формат TSV/массивов подтверждает `test_p81_orchestrator_live.py`).
- **Связь visits↔hits (AC #4):** оба источника грузятся независимо одним `load_day` (`source∈VALID_SOURCES`, своя партиция/строка журнала); связь `watch_ids↔watch_id` — факт каталога, спецкода p81 не несёт (зафиксировано в docstring).

### Live-прогон против реального Logs API — НАХОДКА ДЛЯ РЕВЬЮ (2026-05-25)

> **TL;DR для ревьюера.** Реальный live-smoke вскрыл расхождение **каталог ↔ тариф счётчика**,
> а НЕ дефект оркестратора. Offline-гейты зелёные, логика 2.7 верна. Перед тем как live-smoke
> станет зелёным на этом счётчике, нужно привести `development-docs/schema-catalog.csv` к полям,
> которые тариф разрешает выгружать (отдельная работа по каталогу, не по коду 2.7).

**Сценарий теста (что именно гоняли).** Развёрнуто per-game хранилище `G:/gdau-smoke` (вне
dev-репо), креды в `G:/gdau-smoke/.env`, `GDAU_DATA_ROOT` передаётся в окружении запуска.
Команда: `GDAU_DATA_ROOT="G:/gdau-smoke" uv run pytest -m live tests/test_p81_orchestrator_live.py -v`.
Тест зовёт `ingest_day("visits", вчера)` и `ingest_day("hits", вчера)` против РЕАЛЬНОГО Logs API
счётчика `108178404` (полный прод-путь: writer_lock → write-conn → ensure_load_state_table →
create_views → load_day: create→poll→download→write→verify→mark_loaded→clean), затем сверяет:
партиция создана, `load_state.status='loaded'`, `row_count` == возврату, колонки партиции ⊆
storage-имён каталога.

**Что произошло (факты из `load_state` + сырого ответа API).**
- `test_ingest_day_visits_live` → **FAILED**. `load_state`: `visits / 2026-05-24 / status='failed'`.
  Реальный ответ Logs API на `create_log_request(source="visits", 74 поля каталога)`:
  `400 Bad Request — API Message: "Your tariff doesn't allow to export field ym:s:isRobotPro"`.
  Метрика отклоняет ВЕСЬ запрос на первом запрещённом тарифом поле. visits-выгрузка при этом
  **не создаётся** (поэтому её и не видно в списке `logrequests`).
- `test_ingest_day_hits_live` → стартовал, `load_state`: `hits / 2026-05-24 / status='loading'`;
  запрос `55243779` (41 поле каталога hits) **создан и доведён Метрикой до `processed`** (~54 МБ,
  2 части). Прогон был прерван оператором на фазе скачивания hits (ручная остановка сессии), до
  `mark_loaded`/`clean` не дошло — поэтому hits остался `loading`, а запрос `55243779` висит на
  Метрике непочищенным (его можно скачать как реальный образец для освежения фикстуры hits).

**Вывод по корректности 2.7 (важно для ревью).** Оркестратор отработал **по контракту**: на
ошибке `create` поднял `RuntimeError`, выполнил `mark_failed(visits)` (best-effort) и пробросил
наружу (AC #2, риск №12). Никакого «полу-дня» не записано, целостность не нарушена. Падение —
свойство ДАННЫХ КАТАЛОГА на конкретном тарифе, не логики приёма.

**Открытые follow-up (НЕ в скоупе кода 2.7 — для каталога/новой сессии).**
1. Определить набор visits-полей, разрешённых тарифом счётчика (Метрика ругается на ПЕРВОЕ
   запрещённое — `ym:s:isRobotPro`; после его удаления могут всплыть другие кандидаты:
   `DirectClick*`, `purchase*`/`products*`/ecommerce, `parsedParamsKey*` — проверять итеративно).
2. Привести SSOT `development-docs/schema-catalog.csv` к разрешённым полям (каталог — единственный
   источник списка выгрузки; правка типов/состава — по справочнику, не угадывать).
3. Перезапустить live **с живым логом** (без `| tail` — иначе вывод буферизуется и прогресс/ошибку
   не видно в реальном времени): `… uv run pytest -m live … -v -s --log-cli-level=INFO`.
4. Освежить offline-фикстуры (`logs_visits_sample.tsv`/`logs_hits_sample.tsv`) из реального ответа
   после зелёного прогона (закрытие defer 2.3 — подтверждение wire-формата TSV/массивов).

**Наблюдаемость (вторичный урок).** При live-прогоне через `| tail` вывод pytest буферизуется до
конца процесса — статус опроса и причина падения не видны в реальном времени. Для ручных
live-прогонов использовать `-s --log-cli-level=INFO` (оркестратор логирует статусы через
`logger.info`). Прод-поверхность с печатью прогресса — CLI `gdau-logs update` (2.9).

### File List

- `scripts/8x_metrica_logs_api/p81_load_logs.py` — **новый** (оркестратор: `load_day`/`ingest_day` + хелперы `_poll_until_processed`/`_download_all_parts`/`_parse_parts`/`_require_valid_source`).
- `scripts/utils/row_check.py` — **изменён** (Task 2: вынесен `split_tsv_rows`, `count_part_rows` переписан через него, обновлён модульный docstring + `__all__`; поведение-сохраняющий рефактор).
- `docs/ingestion.md` — **изменён** (Task 3: раздел «Оркестратор приёма за день» — 3 вопроса + границы 2.2/2.3/2.4/2.5/2.6/2.8/2.9/3.1; обновлены вводная и «Границы»).
- `tests/test_p81_orchestrator.py` — **новый** (offline, 18 тестов).
- `tests/test_p81_orchestrator_live.py` — **новый** (live-smoke `@pytest.mark.live`, 2 кейса visits/hits).
- `tests/test_row_check.py` — **изменён** (Task 2: +4 теста `split_tsv_rows` и контракт `count_part_rows == len(split)-1`).
- `tests/fixtures/logs_hits_sample.tsv` — **новый** (мини hits-фикстура).
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — **изменён** (2-7 ready-for-dev → in-progress → review).

**Сессия code-review (2026-05-25) — дополнительно изменены:**

- `scripts/8x_metrica_logs_api/p81_load_logs.py` — **изменён** (3 патча ревью: сверка заголовков частей в `_parse_parts`; `clean` после коммита → `except Exception` по AC #7; диагностика `part_number` в `_download_all_parts`).
- `scripts/utils/parquet_store.py` — **изменён** (perf-фикс: построчный `executemany` → bulk `read_csv` через транзиентный CSV; `_encode_csv_cell`/`_READ_CSV_OPTS`). Вскрыто live-smoke 2.7; правка принадлежит истории 2.2.
- `development-docs/schema-catalog.csv` — **изменён** (сверка с офиц. справочником Logs API: убрано Pro-only `ym:s:isRobotPro`; 19 visits-полей маркетинга/атрибуции получили обязательный префикс `last` — `ym:s:TrafficSource`→`ym:s:lastTrafficSource` + storage `last_*`, зеркало hits — иначе Logs API отклонял их как `Unknown field`).
- `tests/test_p81_orchestrator.py` — **изменён** (+3 регресс-теста на патчи).
- `tests/test_parquet_store.py` — **изменён** (+тест round-trip спецсимволов/`""`↔NULL для bulk-загрузки).
- `tests/test_catalog.py` — **изменён** (снимок-счётчик visits 74→73, всего 115→114 после удаления поля).

## Change Log

- 2026-05-25 — Story 2.7 создана (create-story): оркестратор приёма за день — p81 полный цикл (FR-1, FR-3). Новый модуль `scripts/8x_metrica_logs_api/p81_load_logs.py` — **склейка** примитивов 1.x/2.1–2.6 в протокол идемпотентного дня (create→poll→download→parse TSV→`write_partition`→сверка→`load_state`→clean), НЕ их повтор; **своя** оркестрация (не вендоринг directaiq: без `BaseScript`/`config_manager`/`DROP TABLE`/параллели/warning-сверки). **РЕКОМЕНДОВАН вариант A** (ядро `load_day(conn, client, …)` инъекция + run-level `ingest_day` лок один раз) — **ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ ШЕФА** (риск №1: scope `.writer.lock` на зацикливающем входе, `writer_lock` не реентерабелен; 2.8 берёт лок один раз вокруг диапазона и зовёт `load_day` напрямую). Разобраны риски: единый сплиттер строк p81↔`count_part_rows` (№2, defer 2.3 — поведение-сохраняющий рефактор `row_check.split_tsv_rows`); digit-префикс `8x_…` → `importlib.import_module` (№3, defer 1.1); **live-smoke ОБЯЗАТЕЛЕН** — 2.7 первый end-to-end API после 1.6, фикстуры освежить, закрытие defer 2.3 (№4); порядок/точка коммита `mark_loaded`, clean после = WARNING (№5/№7); частичные части — всё-или-ничего в память (№6); bounded poll константами+kwargs (нет `config_manager`), sleep-шов (№7); пустой день → колонки каталога (№8); колонки = заголовок TSV (№9); валидация дня ≤ вчера, не молчаливый clamp (№10); связь visits↔hits — факт каталога (№11); fail до коммита → `mark_failed`+raise (№12). Дополнение `docs/ingestion.md` (раздел «оркестратор приёма за день»); offline `tests/test_p81_orchestrator.py` (AC #1–#7 + сверка-fail + сплиттер-контракт + пустой день + граница дат + ast-анти-зависимость) + live `tests/test_p81_orchestrator_live.py` + фикстура `logs_hits_sample.tsv`. Статус → ready-for-dev.
- 2026-05-25 — **Решение Шефа по декомпозиции/scope лока: вариант A — УТВЕРЖДЁН.** Вариант B (только чистое `load_day`, весь run-level отложен в 2.8/2.9) отклонён: при нём AC #1 «запускается p81» / AC #3 «под локом» в 2.7 не имеют прод-входа (лишь тест). Вариант A: 2.7 даёт ядро `load_day(conn, client, …)` (инъекция, тестируемо) + run-level `ingest_day(source, date)` (лок+conn+клиент один раз) → AC #1/#3 демонстрируются end-to-end уже в 2.7 + ad-hoc единичный прогон. `writer_lock` не реентерабелен → 2.8 берёт лок один раз вокруг диапазона и зовёт `load_day` напрямую (НЕ `ingest_day` в цикле). Зафиксировано в риске №1, callout «Главные риски», DoD #1; контракт `load_day`/порядок цикла/сплиттер/live-smoke/границы от выбора не зависели — не менялись.
- 2026-05-25 — **Реализация (dev-story): вариант A реализован, все 7 AC закрыты.** Новый `scripts/8x_metrica_logs_api/p81_load_logs.py` — ядро `load_day(conn, client, …)` (инъекция; mark_loading→create→bounded poll→download-всё-в-память→parse→write_partition→verify→mark_loaded[коммит]→clean; clean после коммита = WARNING; fail до коммита → mark_failed best-effort+raise) + run-level `ingest_day` (один захват `.writer.lock`, write-conn, клиент из кредов, `ensure_load_state_table`+`create_views` один раз). Task 2: вынесен `row_check.split_tsv_rows`, `count_part_rows` переписан через него — поведение-сохраняющий рефактор (регресс 2.3 зелёный, defer 2.3 закрыт единым швом). `docs/ingestion.md` дополнен разделом «Оркестратор приёма за день». Тесты: offline `test_p81_orchestrator.py` (18 — AC #1–#7 + сверка-fail + сплиттер-контракт + пустой день + граница дат + невалидный источник + ast-анти-зависимость; поддельный клиент, in-memory conn, sleep no-op, импорт через `importlib`) + `test_row_check.py` (+4) + live `test_p81_orchestrator_live.py` (`@pytest.mark.live`, visits/hits, skip без кредов) + фикстура `logs_hits_sample.tsv`. Гейты: `mypy strict` win32+linux (19 файлов), `pytest` 292 passed/5 deselected, live → 2 skipped (нет кредов в dev-окружении; реальный прогон — ручной), `uv.lock` не менялся. Статус → review.
- 2026-05-25 — **Реальный live-прогон (счётчик 108178404): находка каталог↔тариф.** `visits` падает на `create_log_request` с `400 — "Your tariff doesn't allow to export field ym:s:isRobotPro"` (Метрика отклоняет весь запрос на первом тариф-запрещённом поле); `hits` (41 поле) проходит и доходит до `processed`. Оркестратор отработал по контракту (AC #2: `RuntimeError` → `mark_failed(visits)` → raise; целостность цела, «полу-дня» нет) — **это расхождение данных каталога с тарифом, не дефект кода 2.7** (offline-гейты зелёные). Подробности (сценарий теста, факты из `load_state`, сырой ответ API, follow-up по каталогу, урок наблюдаемости `-s --log-cli-level=INFO`) — в Dev Agent Record → «Live-прогон против реального Logs API — НАХОДКА ДЛЯ РЕВЬЮ». Follow-up (trim каталога до разрешённых тарифом полей + перезапуск live + освежение фикстур) — отдельная работа, вне скоупа кода 2.7.
- 2026-05-25 — **Code-review (bmad-code-review, 3 слоя Opus) + повторный live-прогон: 3 патча 2.7 + perf-фикс 2.2 + trim каталога.** Adversarial-ревью (Blind/Edge/Acceptance): все 7 AC PASS, 3 патча применены к `p81_load_logs.py` (сверка заголовков частей → fail-loud против тихого рассогласования; `clean` после коммита → `except Exception` по AC #7; диагностика `part_number`), +3 регресс-теста. По просьбе Шефа удалено Pro-only поле `ym:s:isRobotPro` из каталога (тариф счётчика его не выгружает) + синхронизирован `test_catalog.py`. Повторный live вскрыл **критичный perf-баг в `parquet_store` (2.2)**: построчный `conn.executemany` вис 25+ мин на реальном дне hits (79 472 строки/41 колонка/~54 МБ) — offline-моки в 3 строки не нагружали. **Исправлено**: bulk-загрузка через транзиентный CSV + нативный `read_csv` (без pandas/pyarrow), round-trip None↔NULL/спецсимволы подтверждён эмпирически (`allow_quoted_nulls=false`); +тест round-trip. **Итог hits: 25+ мин зависание → live PASSED 65s.** Гейты: pytest 296 passed/5 deselected, mypy strict win32+linux 19 файлов, `uv.lock` не менялся. Открытые follow-up (вне кода 2.7, в `deferred-work.md`): visits live красный на следующем тариф-запрещённом поле (вычистить каталог), освежить `logs_hits_sample.tsv` из реального ответа (defer 2.3(b)).
- 2026-05-25 — **Сверка visits-каталога с офиц. справочником Logs API → оба источника live-зелёные.** Разведка показала: visits-краснота — НЕ тариф, а дефект каталога: 19 полей маркетинга/атрибуции (TrafficSource/AdvEngine/Direct*/SocialNetwork*/RecommendationSystem/Messenger) записаны без обязательного префикса модели атрибуции → Logs API отклонял их как `Unknown field in the request`. По справочнику `logs/param` формат — `ym:s:<attribution>TrafficSource`; hits-часть каталога уже была корректна (`ym:pv:lastTrafficSource`). Возвращён префикс `last` (зеркало hits, консистентность источников) + storage `last_*`. **Итог: visits live PASSED 80s (1893×73), hits live PASSED 65s — оба источника доезжают end-to-end.** Гейты: pytest 296/5, mypy strict win32+linux 19 файлов, `uv.lock` не менялся. Остался один follow-up (`deferred-work.md`): освежить рукописную фикстуру `logs_hits_sample.tsv` из реального ответа (defer 2.3(b); реальный контракт уже косвенно подтверждён обоими зелёными live).
